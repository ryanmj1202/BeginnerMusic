rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /collaborationRooms/{roomCode} {
      allow read, write: if roomCode.matches('^[A-Z0-9]{5}$');
    }
  }
}
