# BeginnerMusic 개발 문맥 정리

작성일: 2026-04-16

## 프로젝트 목표

BeginnerMusic은 한국어 사용자, 특히 작곡 입문자가 웹에서 바로 멜로디를 만들 수 있게 하는 초급자용 작곡 편집기다. 1차 목표는 설명형 대시보드가 아니라 실제 작곡이 가능한 피아노 롤 편집기를 완성하는 것이다.

참고 방향은 Signal MIDI의 편집 화면이다. 구조는 상단 메뉴, 왼쪽 트랙 목록, 중앙 피아노 롤, 하단 세부 편집 패널을 따른다. 단순 복제가 아니라 초급자 친화 흐름과 한국어 UI를 유지한다.

## 사용자 요청과 작업 규칙

- 사용자가 직접 해야 하는 작업, 서버 실행, 외부 계정 연결, 배포, Firebase 연결은 Codex가 실행하지 않고 사용자에게 안내한다.
- 개발 서버 실행은 사용자가 직접 한다. 필요한 경우 `npm.cmd run dev`를 안내한다.
- 확인이 필요한 작업은 사용자가 답하는 방식으로 진행한다. 예: "완료가 되었나요?"
- 파일 저장과 불러오기는 JSON 프로젝트 파일과 MIDI 파일을 모두 지원해야 한다.
- 사용자가 JSON 또는 MID/MIDI 파일을 작업 공간에 드래그해 놓으면 바로 불러와야 한다.
- UI는 어두운 Signal MIDI 스타일을 기준으로 다듬는다.

## 현재 구현된 1차 기능

- React, Vite, TypeScript, Tone.js 기반 편집기 화면
- 어두운 Signal MIDI 유사 레이아웃
- 상단 File 메뉴 안의 새 프로젝트, 열기, 프로젝트 저장
- 왼쪽 트랙 목록, 트랙 추가, 트랙 선택, 악기 선택 팝업
- General MIDI 128개 악기 목록 기반 악기 선택
- 중앙 피아노 롤, 피아노 건반, 마디/박자 그리드
- 선택 트랙 음표와 다른 트랙 회색 고스트 음표 표시
- 클릭으로 음표 생성, 기존 음표 선택, 우클릭 삭제
- 음표 길이 조절 핸들
- 하단 Velocity 패널
- localStorage 자동 저장
- JSON 프로젝트 저장/불러오기
- MIDI 파일 불러오기와 MIDI 템포 적용
- 재생/일시정지/정지 버튼
- Space 키로 일시정지와 이어재생
- 건반과 음표 입력 시 누르고 있는 동안 미리듣기

## 이번 이어 작업에서 반영한 요청

- 빈 피아노 롤 칸을 누른 채 드래그하면 음표가 여러 개 찍히지 않고, 하나의 새 음표가 포인터를 따라 이동한다.
- 기존 음표를 누른 채 드래그해서 위치와 음높이를 바꿀 수 있다.
- 드래그 중 음높이가 바뀌면 미리듣기 소리도 같이 바뀐다.
- 건반을 누르는 동안 색이 바뀌고, 누르고 있는 시간만큼 미리듣기 소리가 유지된다.
- 지우개 도구를 추가했다. 지우개 상태에서 클릭한 채 이동하면 지나간 위치의 음표가 삭제된다.
- 빨간 재생 위치 바가 재생 중에도 requestAnimationFrame으로 계속 움직인다.
- JSON, `.mid`, `.midi` 파일을 편집기 위로 드래그 앤 드롭하면 불러온다.

## 2026-04-17 마무리 수정

- 재생 중 빨간 재생 바가 React 상태를 매 프레임 갱신하지 않도록 바꿨다. 이제 피아노 롤 전체를 계속 다시 그리지 않고 CSS 변수만 직접 갱신한다.
- 음표 드래그 미리듣기에서 음높이가 바뀔 때마다 악기를 새로 만들지 않고, 같은 미리듣기 악기에서 현재 음만 바꾸도록 했다.
- 드래그 중 같은 칸에 머무를 때는 불필요한 프로젝트 상태 업데이트를 하지 않게 했다.
- 이 수정은 재생/미리듣기 중 끊기는 느낌을 줄이기 위한 성능 안정화 작업이다.

## 2026-04-17 추가 조작 개선

- 피아노 롤 상단 타임라인을 클릭하면 빨간 재생 위치가 해당 지점으로 이동한다. 재생 중이면 그 위치부터 다시 이어서 재생한다.
- 피아노 건반을 누른 채 다른 건반으로 드래그하면 현재 건반 음이 계속 미리듣기된다.
- 지우개 삭제는 실제 삭제할 음표가 있을 때만 상태를 갱신하도록 바꿔서 반응 속도를 높였다.
- 멜로디 입력 칸의 커서는 더하기/십자 모양이 아니라 기본 마우스 커서로 바꿨다.
- 트랙을 누르면 악기 선택창이 열리고, 악기 선택창은 카테고리와 악기 목록 2열 구조로 표시된다.

## 2026-04-17 성능 최적화

- 자동 저장은 프로젝트 변경 즉시 `localStorage`에 쓰지 않고 500ms 지연 저장으로 바꿨다. 버튼 클릭, 드래그, 삭제 때마다 큰 JSON 저장이 반복되는 문제를 줄이기 위한 조치다.
- MIDI 재생은 모든 음표를 한 번에 예약하지 않고 짧은 구간씩 lookahead 방식으로 예약한다. 긴 MIDI에서 시작 시 멈칫하거나 소리가 끊기는 문제를 줄이기 위한 구조다.
- 피아노 롤 렌더링에서 음표를 매 줄마다 필터링하지 않고, 음높이별 Map으로 미리 묶어 렌더링한다.
- 재생 중 위치 바 갱신은 React 렌더링 없이 CSS 변수만 갱신하는 방식을 유지한다.

## 2026-04-17 긴 MIDI 발열 대응

- 긴 MIDI에서 `36행 × 전체 스텝 수`만큼 버튼을 렌더링하던 구조를 제거했다. 피아노 롤 격자는 CSS 배경으로 그리고, 클릭/드래그 위치는 좌표 계산으로 처리한다.
- 타임라인도 모든 16분음표 칸을 DOM으로 만들지 않고 마디 단위만 렌더링한다.
- MIDI 재생 lookahead는 매번 전체 음표 배열을 필터링하지 않고, 시작 시간이 정렬된 음표를 `nextIndex`로 앞으로만 훑는다.
- Tone.js PolySynth의 최대 동시 발음 수를 줄여 긴 MIDI에서 CPU 부하를 낮췄다.

## 2026-04-17 건반 드래그 미리듣기 수정

- 건반을 누른 채 다른 건반으로 드래그할 때 이전 건반 소리가 계속 울리지 않도록, 미리듣기 악기는 재생용보다 짧은 release를 쓰게 분리했다.
- 미리듣기 음높이가 바뀔 때 특정 음 하나만 release하지 않고 현재 미리듣기 악기의 열린 음을 모두 release한 뒤 새 음을 재생한다.
- release만으로 이전 소리가 남는 문제가 있어, 건반 드래그로 음높이가 바뀌면 이전 미리듣기 악기를 즉시 dispose하고 새 미리듣기 악기로 현재 음만 재생하게 바꿨다.
- 너무 짧게 스치는 미리듣기 음도 최소 0.25초는 재생되도록 했다. 이전 음 정리는 남은 최소 시간만 기다린 뒤 dispose한다.

## 2026-04-17 피아노 롤 레이어와 템포 UI

- 음표 블록이 피아노 건반 영역 위로 겹쳐 보이지 않도록 건반과 코너 셀의 z-index를 높였다.
- 상단 마디 줄에는 기본 클릭 영역 대신 재생 위치 fill과 handle이 보이는 커스텀 타임라인 seek UI를 추가했다.
- BPM 조절은 하단 transport bar에서 빼고 Velocity 패널 오른쪽 위의 별도 템포 패널로 옮겼다.

## 2026-04-17 트랙 선택과 악기 변경 분리

- 트랙 박스를 누르면 악기 변경 창을 열지 않고 해당 트랙을 편집 대상으로 선택한다.
- 선택된 트랙의 멜로디는 기존 파란색 음표로 보이고, 다른 트랙은 회색 고스트 음표로 유지된다.
- 악기 변경 창은 트랙의 둥근 악기 아이콘 또는 상단 악기 pill을 눌렀을 때 열린다.

## 2026-04-17 트랙 목록 스크롤

- 트랙이 많아질 때 왼쪽 패널이 사이트 아래까지 길어지지 않도록 트랙 목록 영역만 내부 스크롤되게 바꿨다.
- 트랙 툴바와 Add Track 버튼은 고정된 영역으로 유지하고, 가운데 트랙 리스트만 스크롤된다.

## 2026-04-17 트랙 우클릭 메뉴

- 트랙을 우클릭하면 한국어 컨텍스트 메뉴가 뜬다.
- 메뉴 항목은 `트랙 추가`, `트랙 삭제`, `속성`, `트랙 색상 변경`이다.
- 마지막 남은 트랙은 삭제되지 않도록 삭제 버튼을 비활성화한다.
- 트랙 색상은 트랙 왼쪽 라인과 둥근 악기 아이콘 테두리에 반영된다.

## 2026-04-17 우클릭 드래그 삭제

- 피아노 롤에서 우클릭을 누른 채 드래그하면 마우스가 지나간 위치의 음표를 삭제한다.
- 삭제 대상은 현재 선택된 트랙의 음표로 제한한다. 다른 트랙의 회색 고스트 음표는 삭제하지 않는다.
- 음표 블록 위에서 우클릭을 시작해도 삭제 모드가 시작되며, 이어서 드래그하면 닿는 음표들이 삭제된다.

## 2026-04-17 악기 이미지 에셋

- 트랙의 둥근 악기 버튼과 상단 악기 pill에 텍스트 기호 대신 로컬 악기 이미지 에셋을 표시한다.
- 에셋은 `public/instrument-icons`에 SVG 이미지로 저장했다.
- General MIDI 악기군에 따라 피아노, 드럼, 기타, 베이스, 현악, 브라스, 목관, 오르간, 퍼커션, 신스, FX 이미지로 자동 매핑된다.

## 2026-04-17 템포 수정 개선

- 하단 템포 패널의 BPM 입력값이 변경될 때 프로젝트 템포에 바로 반영되도록 했다.
- 자주 쓰는 BPM 프리셋을 select로 선택할 수 있게 추가했다.

## 2026-04-17 재생 바 자동 추적

- 재생 중 빨간 재생 바가 피아노 롤 화면 오른쪽 밖으로 나가려 하면 피아노 롤 가로 스크롤이 자동으로 따라가도록 했다.
- 타임라인을 클릭해 재생 위치를 옮길 때도 해당 위치가 보이도록 스크롤을 맞춘다.

## 2026-04-17 우클릭 삭제 상태 버그 수정

- 우클릭 삭제 후 버튼을 뗐는데도 마우스를 올리면 음표가 삭제되는 문제를 막았다.
- 오른쪽 버튼이 실제로 눌려 있지 않으면 우클릭 삭제 상태를 즉시 해제한다.
- `pointerup`, `pointercancel`, `contextmenu`, `blur`에서도 드래그/삭제 상태를 모두 초기화한다.

## 2026-04-17 추가 렌더링 최적화

- 전역 `keydown`, `pointermove`, `pointerup` 등의 이벤트 리스너가 매 렌더마다 다시 붙지 않도록 핸들러 ref 기반으로 정리했다.
- 선택 트랙 음표, 다른 트랙 음표, 선택 음표, 악기 카테고리/목록 같은 파생 데이터를 `useMemo`로 묶어 불필요한 재계산을 줄였다.
- 제목, 템포, 트랙 선택, 트랙 속성 갱신 시 값이 실제로 바뀌지 않으면 상태를 다시 세팅하지 않게 했다.

## 2026-04-18 작업 기록 규칙

- 앞으로 기능 수정, UI 변경, 버그 수정, 구조 변경을 하면 기존 `.codex/project-context.md` 파일의 작업 기록 영역에 이어서 기록한다.
- 기존 내용을 삭제하지 않고, `향후 2차 개발 메모` 위에 날짜별 제목과 bullet 목록 형식으로 추가한다.
- 기록에는 사용자가 요청한 의도, 실제로 바꾼 동작, 다음 작업자가 알아야 할 주의점을 짧게 남긴다.

## 2026-04-18 악기 선택, 음표 단위, 피아노 롤 조작 개선

- 악기 선택창에서 악기를 누르면 선택 후보가 바뀌고 해당 악기의 미리듣기 소리가 재생되도록 했다.
- 악기 목록과 악기 카테고리 목록에 도형 대신 `public/instrument-icons` SVG 이미지를 표시한다.
- 음표 단위는 `8`, `16`, `32`, `64`, `128`분음표를 선택할 수 있고, 선택한 단위에 맞춰 새 음표 길이, 이동, 리사이즈 스냅이 바뀐다.
- 음표 단위 버튼에는 텍스트 기호 대신 `public/note-icons` SVG 박자 아이콘을 사용한다.
- 피아노 롤은 기본 8마디에서 시작하고, 필요한 길이에 따라 8, 16, 32, 64처럼 2배 단위로 확장된다.
- 브라우저 확대/축소 대신 피아노 롤 자체 확대/축소를 제공한다. 도구줄의 `-`/`+`, `Ctrl + 휠`, `Ctrl + +`, `Ctrl + -`가 피아노 롤 줌을 조절한다.
- 스페이스바는 입력창 포커스 여부와 관계없이 항상 재생/일시정지 조작으로 처리한다.
- Tone.js 악기 생성은 General MIDI 카테고리뿐 아니라 program 번호별 oscillator, envelope, FM 파라미터를 다르게 적용한다. 실제 SoundFont는 아니므로 완전한 실악기 음색은 추후 샘플 기반 재생으로 개선해야 한다.

## 2026-04-18 피아노 롤 렉 최적화

- 드래그 이동과 리사이즈 중 같은 위치/같은 길이로 반복 입력되는 포인터 이벤트는 프로젝트 상태를 다시 갱신하지 않게 했다.
- 음표 선택, 기존 음표 클릭, velocity bar 클릭도 이미 선택된 음표라면 불필요한 `setProject`를 하지 않도록 했다.
- 편집 중 자동 저장 지연 시간을 늘려 드래그 중 `localStorage` JSON 직렬화가 끼어드는 빈도를 줄였다.
- 피아노 롤 드래그 시작 시 그리드 좌표와 폭을 캐싱하고, 포인터 이동 중 반복적인 `getBoundingClientRect()` 호출을 줄였다.
- 피아노 롤, step row, note block, velocity lane에 CSS `contain`과 필요한 `will-change`를 적용해 브라우저 레이아웃/페인트 범위를 줄였다.
- 여전히 매우 큰 MIDI에서 렉이 남으면 다음 단계는 음표 DOM 가상화 또는 드래그 중 임시 transform 표시 후 pointerup에서 상태 확정하는 방식이 필요하다.

## 2026-04-18 용어 설명 1차 구현

- `향후 2차 개발 메모`의 첫 항목인 용어 설명을 먼저 구현했다.
- 하단 세부 편집 탭에 `Velocity`, `Pitch Bend`, `Volume`, `Panpot`, `Expression`, `Modulation`의 쉬운 한국어 이름을 함께 표시한다.
- 하단 velocity lane 위에 용어 설명 strip을 추가해 `소리 세기`, `음높이 휘기`, `트랙 음량`, `좌우 위치`, `연주 느낌`, `떨림`의 의미를 짧게 안내한다.
- 현재는 설명 UI만 연결되어 있고, Velocity 외의 탭 기능은 아직 실제 편집 기능과 연결되지 않았다.

## 2026-04-18 번역 목록 파일 추가

- 사용자가 직접 번역을 채울 수 있도록 `translation-list.md` 파일을 추가했다.
- 상단 메뉴, 파일 UI, 악기 선택창, 트랙 UI, 피아노 롤 도구, 세부 편집 용어, 악기 카테고리, General MIDI 128개 악기명, 브라우저 메타데이터 문자열을 표 형태로 정리했다.
- `ko` 칸은 비워 두었고, 사용자가 번역을 채운 뒤 다시 불러와 앱 문자열에 반영하는 흐름으로 진행한다.

## 2026-04-18 번역 입력용 CSV 추가

- 마크다운 표는 직접 입력하기 불편하므로 `translation-input.csv` 파일을 추가했다.
- CSV는 `section`, `id`, `en`, `ko` 열로 구성되어 스프레드시트에서 열어 `ko` 열만 채우기 쉽게 했다.
- 기존 `translation-list.md`는 참고용으로 유지하고, 실제 번역 입력은 `translation-input.csv`를 우선 사용한다.

## 2026-04-18 번역 입력용 Excel 파일 추가

- 사용자가 CSV도 불편하다고 요청해 `translation-input.xlsx` 파일을 추가했다.
- Excel 파일은 `Translations` 시트에 `section`, `id`, `en`, `ko` 열을 두고, 헤더 고정과 자동 필터, 넓은 열 너비를 적용했다.
- 사용자는 `ko` 열만 채워 저장하면 되고, 이후 이 파일을 읽어 앱 문자열에 반영하는 방식으로 진행한다.

## 2026-04-18 편집용 번역 Excel 호환 파일 추가

- 기존 `translation-input.xlsx`가 Excel에서 입력이 잘 되지 않는다고 해서 `translation-input-editable.xls` 파일을 추가했다.
- 새 파일은 HTML 기반 Excel 호환 표이며, `ko - 여기에 입력` 열을 노란색으로 표시했다.
- Excel에서 형식 경고가 뜰 수 있지만, 열어서 `ko` 열을 입력하고 저장하는 편집용 파일로 사용한다.

## 2026-04-18 브라우저 번역 편집기 추가

- 사용자가 OneDrive/Excel 없이 번역을 입력할 수 있도록 `translation-editor.html` 파일을 추가했다.
- 브라우저에서 열어 `translation-input.csv`를 불러오고, `ko` 입력 칸을 채운 뒤 `translation-input-filled.csv`로 저장하는 방식이다.
- 로컬 파일 자동 읽기가 브라우저 정책으로 막힐 수 있으므로, 필요하면 `CSV 불러오기` 버튼으로 `translation-input.csv`를 직접 선택한다.

## 2026-04-18 보이는 UI 문자열 번역 목록 정리

- 사용자가 실제 UI에 보이는 문자열만 번역하고 싶다고 요청해 `translation-visible-input.csv`와 `translation-visible-list.md`를 추가했다.
- 내부 설명, 접근성 라벨, 브라우저 메타데이터, 보이지 않는 tool title 등은 제외하고 화면에 직접 표시되는 메뉴, 버튼, 탭, 카테고리, General MIDI 악기명 중심으로 정리했다.
- `translation-editor.html`의 기본 불러오기와 저장 파일명도 visible-only 목록 기준으로 변경했다.

## 2026-04-18 악기 제외 UI 번역 목록 정리

- 사용자가 악기명 번역은 Codex가 한국인에게 친화적인 표현으로 처리하고, 본인은 악기 외 옵션만 번역하겠다고 요청했다.
- `translation-ui-only-input.csv`와 `translation-ui-only-list.md`를 추가해 상단 메뉴, 악기 선택창 UI, 트랙 UI, 세부 편집/재생 UI만 남겼다.
- General MIDI 악기명과 악기 카테고리명은 사용자 입력 목록에서 제외했다. 이후 앱 반영 시 악기명은 `통기타`, `전자피아노`처럼 자연스러운 한국어 이름으로 Codex가 별도 처리한다.
- `translation-editor.html`의 기본 불러오기와 저장 파일명도 UI-only 목록 기준으로 변경했다.

## 2026-04-18 SoundFont 악기 로딩 1차 적용

- Tone.js 합성음만으로는 악기 차이가 부족하므로 General MIDI 악기에 SoundFont 샘플을 우선 사용하는 구조를 추가했다.
- `midi-js-soundfonts`의 FluidR3_GM 샘플 URL을 사용하며, 각 GM 악기명을 SoundFont 폴더명으로 변환해 Tone.Sampler로 로드한다.
- 샘플 로딩 전이나 실패 시에도 소리가 끊기지 않도록 기존 Tone.js 합성음을 fallback 악기로 유지한다.
- 악기 미리듣기와 재생 시작은 SoundFont 로딩을 최대 1.5초 기다리고, 이후에는 fallback 또는 로드된 샘플로 재생한다.
- 드럼과 GM 112번 이후 일부 효과음은 우선 기존 합성음 fallback을 사용한다. 실제 배포 품질을 높이려면 추후 샘플을 로컬에 저장하거나 SoundFont 소스 선택을 확정해야 한다.

## 2026-04-18 장시간 사용 렉 완화 및 SoundFont 캐시 개선

- SoundFont 악기를 미리듣기/재생마다 새로 만들지 않고 악기명별 Tone.Sampler 캐시를 재사용하도록 바꿨다.
- 캐시는 최대 16개 악기까지 유지하고, 사용 중이 아닌 오래된 Sampler부터 정리하는 LRU 방식으로 메모리 증가를 제한한다.
- 한 번 실패한 SoundFont는 실패 목록에 저장해 같은 악기를 반복해서 다시 다운로드하지 않게 했다.
- 악기 선택 미리듣기는 새 미리듣기를 시작할 때 이전 one-shot 미리듣기를 즉시 release/dispose해 오디오 객체와 타이머가 누적되지 않게 했다.
- SoundFont 로딩 대기 timeout은 준비가 먼저 끝나면 타이머를 즉시 해제하도록 바꿔 장시간 사용 시 불필요한 timeout 누적을 줄였다.
- 재생 시작 중 SoundFont 로딩을 기다리는 동안 사용자가 다시 재생/정지/탐색하면 이전 비동기 시작 요청이 뒤늦게 interval을 만드는 문제를 막기 위해 playback session 토큰을 추가했다.
- 자동 저장은 직렬화된 프로젝트 JSON이 실제로 바뀐 경우에만 `localStorage`에 쓰도록 해 반복 저장 비용을 줄였다.
- GM 112~127 악기도 SoundFont 이름 변환 대상에 포함해 더 많은 General MIDI 악기가 샘플 로딩을 시도하도록 했다. `drums` 전용 트랙은 기존 합성 드럼 fallback을 유지한다.

## 2026-04-18 피아노 롤 장시간 렉 원인 분석 및 추가 최적화

- 렉 원인 후보를 `App.tsx`, `App.css`, `toneTransport.ts`, MIDI/악기 관련 파일 기준으로 다시 분석했다.
- 피아노 롤의 각 피치 줄마다 반복 그리드 배경과 재생선이 따로 그려져 128분음표/긴 마디/재생 중 페인트 비용이 커지는 문제가 있어, 그리드 배경과 재생선을 단일 오버레이로 합쳤다.
- 모든 음표 블록에 상시 적용되던 `will-change`를 제거하고, 실제 이동/리사이즈 중인 음표에만 적용되게 해 브라우저 레이어와 GPU 메모리가 오래 누적될 여지를 줄였다.
- 드래그 이동과 드래그 삭제의 `pointermove` 처리를 즉시 상태 갱신하지 않고 `requestAnimationFrame` 단위로 묶어, 고주사율 마우스/트랙패드에서 React 갱신이 과도하게 발생하지 않게 했다.
- 음표 길이 리사이즈도 `requestAnimationFrame` 단위로 처리하고, 포인터를 놓을 때 마지막 위치를 반영하도록 했다.
- 프로젝트 전체 끝 박자 계산은 트랙별 음표 배열 참조를 `WeakMap`으로 캐시해, 한 트랙만 편집할 때 다른 트랙의 모든 음표를 반복 스캔하지 않게 했다.
- 다른 트랙 고스트 음표의 피치별 묶음도 트랙별 음표 배열 참조가 바뀌지 않았으면 이전 계산 결과를 재사용하도록 했다.
- SoundFont 캐시 최대치를 16개에서 8개 악기로 낮추고, 3분 이상 쓰지 않은 Sampler는 정리하도록 해 오래 켜둘 때 오디오 버퍼 메모리 증가를 더 강하게 제한했다.
- SoundFont 샘플 포인트를 C2~C6 중심으로 줄여 악기 하나를 불러올 때 다운로드/디코딩해야 하는 샘플 수를 줄였다.
- 악기 선택창에서 빠르게 여러 악기를 눌렀을 때 이전 비동기 미리듣기 요청이 뒤늦게 재생되지 않도록 one-shot preview 토큰을 추가했다.

## 2026-04-18 드래그 패턴 선택 도구 추가

- 피아노 롤 도구에 `드래그 패턴 선택` 도구를 추가했다.
- 선택 도구를 켠 상태에서 피아노 롤 빈 영역을 드래그하면 반투명 선택 박스가 표시된다.
- 선택 박스와 겹치는 현재 선택 트랙의 음표들이 한 번에 강조 표시되며, 첫 번째 선택 음표는 기존 단일 선택 상태와 연결된다.
- 선택 도구 상태에서 음표를 직접 누르면 이동하지 않고 해당 음표만 선택되도록 해, 기존 그리기/이동 도구와 역할을 분리했다.
- 현재는 패턴 선택과 하이라이트까지 구현되어 있으며, 복사/반복/그룹 이동 같은 패턴 편집 기능은 이후 이 선택 상태를 기반으로 확장하면 된다.

## 2026-04-18 패턴 선택 후 편집 기능 연결

- 드래그 선택된 여러 음표를 잡고 움직이면 선택된 음표 묶음이 함께 이동하도록 했다.
- 선택 묶음 이동은 피아노 롤 범위를 벗어나지 않게 음높이와 시작 위치를 클램프한다.
- 상단 `Edit` 메뉴를 실제 메뉴로 바꾸고 `Copy`, `Cut`, `Paste`, `Duplicate`를 연결했다.
- 키보드 단축키 `Ctrl+C`, `Ctrl+X`, `Ctrl+V`로 선택 음표 복사, 자르기, 붙여넣기가 가능하다.
- `Delete`/`Backspace`는 입력창 포커스가 아닐 때 선택된 단일 음표 또는 패턴 선택 음표 묶음을 삭제한다.
- 붙여넣기는 현재 재생 위치가 있으면 그 위치에 붙이고, 재생 위치가 0이면 복사한 패턴 뒤쪽에 이어 붙인다.

## 2026-04-18 박스 선택 사용성 개선

- 박스 선택 후 마우스를 놓아도 선택 영역 박스가 바로 사라지지 않고 남도록 바꿨다.
- 선택 도구 상태에서 이미 선택된 음표를 누르면 단일 선택으로 바뀌지 않고 그대로 묶음 이동을 시작할 수 있게 했다.
- 선택 도구로 선택되지 않은 음표를 누르면 해당 음표만 선택하고, 이미 선택된 음표를 다시 끌면 이동하는 흐름으로 정리했다.
- 그리기 도구 상태에서도 `Shift + 드래그`로 박스 선택을 시작할 수 있게 해 도구 전환 부담을 줄였다.
- 새 선택, 삭제, 붙여넣기, 도구 변경처럼 선택 영역이 더 이상 맞지 않는 동작에서는 선택 박스를 정리한다.

## 2026-04-18 복사/붙여넣기 스냅, 곡 길이, 음표 길이 기억 개선

- 복사한 패턴을 붙여넣을 때 현재 음표 단위 격자에 맞춰 시작 위치와 음표 위치가 스냅되도록 했다.
- 프로젝트 데이터에 `lengthBeats`를 추가하고, 하단 템포 패널에서 `길이(초)`를 직접 입력하거나 `+10`으로 늘릴 수 있게 했다.
- 피아노 롤 길이는 실제 음표 끝과 사용자가 지정한 곡 길이 중 더 긴 값을 기준으로 확장된다.
- 새 음표 입력 길이는 현재 음표 단위의 기본값만 쓰지 않고, 가장 최근에 선택하거나 리사이즈한 음표 길이를 기억해서 적용한다.
- 음표 단위를 바꿔도 기억한 길이를 현재 격자에 맞춰 다시 스냅해 새 음표 크기에 반영한다.
- 재생 정지/일시정지 시 악기에 `triggerRelease`를 먼저 호출하고 dispose하며, lookahead 예약 구간을 줄여 멈춘 뒤 소리가 조금 더 들리는 현상을 줄였다.

## 2026-04-18 재생 정지 안정화, MIDI 저장, 자동 믹스 1차 구현

- 재생 예약 방식을 Tone.js 미래 시각 직접 예약에서 브라우저 timeout 기반 예약으로 바꿨다.
- 정지/일시정지 시 `activeTimeoutsRef`의 시작/릴리즈 예약을 모두 취소하므로, 멈춘 뒤 예약된 음이 계속 튀어나오는 현상을 줄였다.
- `src/lib/midi/exportMidi.ts`를 추가해 프로젝트의 템포, 박자, 트랙 이름, GM program, 음표 데이터를 표준 MIDI 파일로 저장할 수 있게 했다.
- File 메뉴에 `Save MIDI`를 추가했다. 저장 파일 확장자는 `.mid`이다.
- 상단 작업 영역에 `Auto Mix` 버튼을 추가했다.
- 자동 믹스 1차 버전은 트랙별 음표 길이, velocity, 밀도를 기준으로 트랙 volume을 0~1 범위에서 자동 조정한다.
- MP3 저장은 현재 프로젝트 의존성에 MP3 인코더가 없어 이번 구현에는 포함하지 않았다. 추후 `lamejs` 같은 인코더를 추가하거나 브라우저 녹음/인코딩 방식을 결정해야 한다.

## 2026-04-18 Undo/Redo, 피아노 롤 우클릭 메뉴, 줌 동작 개선

- 프로젝트 변경 시 이전 상태를 최대 80개까지 undo 스택에 저장하도록 했다.
- `Ctrl+Z`는 실행 취소, `Ctrl+Shift+Z`와 `Ctrl+Y`는 다시 실행으로 연결했다.
- 상단 `Edit` 메뉴에 `Undo`, `Redo`를 추가했다.
- 피아노 롤에서 우클릭하면 실행 취소, 다시 실행, 복사, 잘라내기, 붙여넣기, 삭제 메뉴가 뜨도록 했다.
- 피아노 롤 우클릭은 편집 메뉴를 우선 띄우도록 바꿔 기존 우클릭 즉시 삭제 동작과 충돌하지 않게 했다.
- `Ctrl + 휠` 확대/축소는 native wheel listener에서 `preventDefault`를 걸어 브라우저 전체 확대가 아니라 피아노 롤 줌만 바뀌도록 조정했다.

## 2026-04-19 MP3 내보내기와 오케스트라 SoundFont 소스 개선

- `lamejs`를 추가해 브라우저 안에서 MP3 파일을 인코딩할 수 있게 했다.
- `src/lib/audio/exportMp3.ts`를 추가했다. 현재 프로젝트 음표를 `OfflineAudioContext`로 렌더링한 뒤 MP3로 인코딩한다.
- File 메뉴에 `Save MP3`를 추가했다. 내보내기 중에는 `Exporting MP3...`로 표시한다.
- 현재 MP3 렌더링은 SoundFont 샘플을 그대로 녹음하는 방식이 아니라, 프로젝트 음표를 간단한 WebAudio oscillator 기반 음색으로 오프라인 렌더링하는 1차 구현이다.
- SoundFont 조사 결과, 현재 구조에 바로 연결 가능한 MIDI.js SoundFonts 계열에는 `FluidR3_GM`, `MusyngKite`, `FatBoy`가 있으며, 그중 오케스트라 계열 악기에는 `MusyngKite`를 우선 적용했다.
- `toneTransport.ts`에서 GM 40~79번(현악, 앙상블, 브라스, 리드, 파이프 계열)은 `MusyngKite` SoundFont URL을 사용하고, 나머지는 기존 `FluidR3_GM`을 유지한다.
- 더 본격적인 오케스트라 라이브러리로는 MuseScore General/HQ, Sonatina Symphonic Orchestra, Virtual Playing Orchestra가 있지만, SF2/SF3/SFZ 기반이라 현재 Tone.Sampler mp3 URL 로딩 방식에는 별도 변환/샘플 파이프라인이 필요하다.

## 2026-04-19 MP3 내보내기 실패 수정

- `lamejs` npm main 파일이 런타임에서 `MPEGMode is not defined` 오류를 내는 문제가 있어 MP3 저장이 실패했다.
- `src/js/index.js` 기반 import 대신 패키지에 포함된 `lame.min.js` 브라우저 번들을 raw 문자열로 불러와 한 번만 초기화하는 방식으로 변경했다.
- 별도 `lamejs` 타입 선언 파일은 제거하고, `exportMp3.ts` 내부에서 필요한 최소 타입만 정의했다.
- 이 수정 후 `tsc`, `lint`, `build`가 모두 통과했다.

## 2026-04-19 피아노 롤 우클릭 동작 분리

- 피아노 롤 빈 공간 우클릭은 오른쪽 버튼을 누르는 순간이 아니라 버튼을 뗄 때 편집 메뉴가 뜨도록 변경했다.
- 음표 위에서 우클릭하면 편집 메뉴를 열지 않고 해당 음표를 바로 삭제하도록 했다.
- 음표 위에서 우클릭한 채 드래그하면 지나간 선택 트랙 음표들이 삭제되며, 이 경우 편집 메뉴는 뜨지 않는다.
- 빈 공간에서 우클릭을 시작해 드래그로 음표를 삭제한 경우도 삭제 의도로 보고 편집 메뉴를 띄우지 않는다.
- 우클릭 이동 거리가 일정 이상이면 단순 클릭이 아니므로 메뉴를 띄우지 않게 했다.

## 2026-04-19 겹친 음표 재생 누락 수정

- 같은 트랙에서 같은 음높이 음표가 겹칠 때 먼저 끝난 음표의 release가 뒤 음표를 같이 끊는 문제가 있었다.
- 재생 스케줄러의 `ActivePlaybackTrack`에 `activeFrequencies` 카운터를 추가했다.
- 같은 frequency가 여러 번 열려 있으면 첫 attack만 실행하고, 마지막 겹친 음표가 끝날 때만 release하도록 했다.
- SoundFont 래퍼 내부의 `activeNotes`도 `Set`에서 `Map<number, number>` 카운터로 바꿔 같은 음높이 중첩을 안전하게 처리한다.

## 2026-04-19 음표 드래그 시작점 점프 수정

- 기존에는 음표의 중간이나 끝부분을 잡아도 마우스가 있는 칸이 음표의 새 시작점이 되어 음표가 즉시 점프했다.
- `NoteDrag`에 `grabStepOffset`을 추가해 음표 안에서 클릭한 위치와 음표 시작점 사이의 칸 차이를 기억한다.
- 드래그 중에는 `포인터 칸 - grabStepOffset`을 새 시작점으로 사용해, 잡은 위치가 그대로 유지된 채 움직인다.
- 단일 음표 이동과 박스 선택된 묶음 이동 모두 같은 오프셋 보존 방식을 사용한다.

## 2026-04-19 트랙, 드럼, 하단 편집 패널 개선

- 트랙 목록 빈 공간을 우클릭하면 한국어 트랙 메뉴가 뜨도록 했다. 기존 개별 트랙 우클릭 메뉴와 같은 항목을 사용한다.
- 트랙 박스는 선택 용도로 유지하고, 악기 변경은 둥근 악기 이미지 버튼과 상단 악기 pill에서만 열리도록 유지했다.
- 트랙별 `channel` 값을 실제 프로젝트 데이터에 저장하고, MIDI 내보내기에서는 선택 채널을 반영한다. 드럼 트랙은 General MIDI 규칙에 맞춰 10번 채널을 유지한다.
- AutoMix 버튼은 상단에서 제거하고 트랙 목록 아래쪽으로 이동했다.
- 드럼 트랙 선택 시 피아노 롤 행은 건반 위치를 유지하되 Kick, Snare, Hat, Tom, Crash 같은 드럼 키트 이름으로 표시한다.
- 드럼 미리듣기와 재생은 기존 삐 소리 대신 kick/snare/hat/tom/cymbal 계열로 분리한 Tone.js 드럼 키트 fallback을 사용한다.
- 선택 박스 자체를 누르고 드래그해 선택된 음표 묶음을 바로 이동할 수 있게 했다.
- 하단 Velocity 막대 그래프와 설명 가이드는 제거하고, 선택한 음표 또는 박스 선택된 음표 묶음에 적용되는 단일 컨트롤 패널로 바꿨다.
- 하단 패널에서 Velocity, Volume, Pan, Pitch Bend, Expression, Modulation을 실제 음표 데이터에 저장하고 선택 묶음 전체에 한 번에 적용한다.
- MP3 출력에는 선택 음표의 Pan 값이 stereo panner로 반영된다.
- MIDI 출력에는 음표별 Volume(CC7), Pan(CC10), Expression(CC11), Modulation(CC1), Pitch Bend 이벤트를 함께 기록한다.
- 음표 단위 버튼은 피아노 롤 확대/축소가 아니라 새 음표 길이, 이동, 리사이즈 스냅 정밀도만 바꾸도록 했다. 별도 피아노 롤 확대/축소 UI와 Ctrl+휠 확대는 제거했다.
- 프로젝트 길이 입력 UI를 제거하고, 피아노 롤은 마지막 음표 위치를 기준으로 8/16/32/64마디처럼 자동 확장된다. MP3 출력 길이도 수동 길이 대신 마지막 음표 끝을 기준으로 잡는다.
- 멜로디 음표 블록 안에 `C5` 같은 음높이 이름을 표시한다. 피아노 건반 라벨은 C 계열만 표시해 건반처럼 덜 복잡하게 보이도록 했다.
- 프로젝트 저장 파일명은 `.json`으로 저장하고, MIDI를 불러온 프로젝트 제목은 확장자를 뺀 파일 이름으로 설정한다.
- File/Edit 버튼의 화살표와 버튼 폭을 정리했다.
- 상단 Piano Roll, Arrange, Tempo 탭을 실제 탭 전환으로 연결했다. Arrange는 트랙별 배치 보기, Tempo는 그래프 클릭으로 BPM을 조절하는 1차 화면이다.
- 이번 수정 후 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다.

## 2026-04-19 큰 편집 기능 1차 구현

- 선택 도구에 자유 선택(lasso) 모드를 추가했다. `⌁` 버튼을 누르거나 `Alt`를 누른 상태로 피아노 롤에서 그리면 마우스로 그린 영역 안의 현재 트랙 음표가 선택된다.
- 박스 선택으로 만든 선택 영역 자체를 움직이는 기능을 유지하면서, 선택 영역 오른쪽 손잡이를 드래그하면 선택된 패턴을 오른쪽으로 반복 복제할 수 있게 했다.
- 재생 중 실시간 키보드 입력 기능을 추가했다. `Keys` 버튼을 켠 뒤 재생 중 `A W S E D F T G Y H U J K` 키를 누르면 선택 트랙에 음표가 기록된다.
- 드럼 트랙에서 키보드 입력을 사용할 때는 일반 음계 대신 드럼 키트 행에 맞춰 입력된다.
- 하단 편집 탭에 `Event / 이벤트`를 추가했다. 선택한 음표 또는 선택 묶음의 음높이, 시작 박자, 길이, 세기를 숫자로 직접 수정할 수 있다.
- Event 편집은 현재 선택 음표 중 최대 12개를 표시해 큰 MIDI에서 하단 패널이 과하게 무거워지지 않게 했다.
- 선택 음표 배열 계산을 `useMemo`로 묶어 렌더링 경고를 제거했다.
- 이번 수정 후 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다.

## 2026-04-19 선택/반복/드럼/키 입력 보정

- 자유 선택(lasso) 중 그려지는 선, 반투명 채움, 점들이 피아노 롤 위에 보이도록 SVG 오버레이를 강화했다.
- 선택 박스를 드래그해 선택 음표 묶음을 이동할 때 박스 좌표도 함께 갱신되도록 했다.
- 패턴 반복 손잡이의 현재 폭을 ref에 저장해 pointerup 시점에 최신 폭으로 반복 횟수를 계산하도록 고쳤다.
- 선택 박스를 오른쪽으로 늘려 패턴 반복을 만들면 새로 생성된 음표까지 포함해 선택 박스가 다시 잡히도록 했다.
- 실시간 키보드 입력 범위를 늘렸다. 아래 줄, 홈 줄, 숫자 줄을 함께 사용해 더 넓은 음역을 입력할 수 있다.
- 재생 가능한 음표/악기가 없는 빈 프로젝트에서도 재생 상태가 시작되도록 바꿨다. 이 상태에서 `Keys` 입력으로 새 음표를 바로 기록할 수 있다.
- `Keys` 옆의 더미 음표 버튼을 제거하고, Keys 버튼에는 키보드 기호를 붙였다.
- AutoMix 버튼 안에 `fx.svg` 이미지를 추가해 트랙 버튼들과 시각적으로 더 맞게 만들었다.
- 드럼 입력이 MIDI 번호와 주파수를 혼동하던 문제를 고쳤다. 이제 35~81 GM drum note를 그대로 해석한다.
- 드럼 fallback을 kick, snare, hi-hat, tom, cymbal 계열별로 다른 Tone.js 합성 구조로 분리했다. SoundFont 직접 드럼 샘플은 현재 브라우저 mp3 URL 구조에서 안정적으로 연결하기 어려워 fallback을 먼저 개선했다.
- SoundFont 조사 메모: midi-js-soundfonts는 브라우저 직접 로딩이 쉽지만 원본 계열에서 percussion track/channel 10 지원이 제한적이다. 실제 샘플 드럼은 GeneralUser GS 같은 SF2/GS 은행을 별도 변환해 로컬 mp3 샘플 매핑으로 넣는 방향이 더 적합하다.
- 이번 수정 후 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다.

## 2026-04-19 음악 길이와 드럼 소리 추가 보정

- 피아노 롤 편집 공간은 마지막 음표 뒤에 항상 128마디 여유를 붙이도록 했다. 새 음표를 뒤쪽에 추가하면 마지막 음표 기준이 다시 늘어나므로 계속 앞으로 작업할 수 있다.
- MP3 출력 길이는 수동 프로젝트 길이나 편집 여유 구간을 쓰지 않고 마지막 음표 끝 기준에 0.15초만 더해 바로 끝나게 했다.
- 드럼 fallback을 더 세분화했다. kick, snare, side stick, clap, closed/open hat, tom, crash/ride, tambourine, cowbell, vibraslap, bongo/conga/timbale, agogo, cabasa/maracas, whistle, guiro, clave/wood block, cuica, triangle이 서로 다른 합성 경로를 사용한다.
- Crash 1(49)이 tom으로 처리되던 순서 문제를 고치고, Vibraslap(58)이 cymbal 범위에 먼저 잡히던 문제도 분리했다.
- 이번 수정 후 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다.

## 2026-04-19 Power Drum, 피아노 롤 확대, 메뉴 닫힘 복구

- 피아노 롤 전용 확대/축소를 복구했다. 상단 도구줄의 `-`, `+` 버튼과 `Ctrl + 휠`, `Ctrl + +`, `Ctrl + -`가 사이트 전체가 아니라 피아노 롤 beat width만 바꾼다.
- 우클릭 편집 메뉴는 다른 곳을 왼쪽 클릭하면 capture 단계에서 닫히도록 했다. `Esc` 키로도 트랙/피아노 롤 컨텍스트 메뉴가 닫힌다.
- Power Drum 느낌을 강화하기 위해 kick에 저역 body와 짧은 click/noise 레이어를 추가하고, snare에는 밝은 snap 레이어를 추가했다.
- hi-hat과 crash/ride는 더 긴 decay와 높은 resonance/metal layer를 써서 더 시원하게 퍼지도록 했다.
- 이번 수정 후 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다.

## 2026-04-19 전체 선택과 패턴 반복 스냅 개선

- `Ctrl+A`를 누르면 현재 선택된 트랙의 모든 멜로디가 선택되도록 했다. 입력창이 포커스되어 있어도 피아노 롤 편집 단축키로 처리한다.
- 전체 선택 후 선택 박스가 선택된 멜로디 범위에 맞게 다시 잡히므로, 바로 복사/자르기/삭제/반복 작업을 이어갈 수 있다.
- 패턴 반복 손잡이를 드래그할 때 선택 박스 폭이 패턴 1회, 2회, 3회처럼 원본 패턴 폭 단위로 스냅되도록 바꿨다.
- 반복 복제도 미리보기 폭을 내림 계산하지 않고 스냅된 반복 횟수를 그대로 사용해, 손잡이 위치와 실제 생성 결과가 어긋나지 않게 했다.
- 드럼 fallback에서 Vibraslap(58)이 cymbal 범위보다 먼저 처리되도록 순서를 고쳐 해당 드럼 음이 고유한 소리로 나게 했다.

## 2026-04-19 구간별 우선순위 AutoMix와 용어 Excel

- 프로젝트 데이터에 `autoMixSections`를 추가했다. 각 구간은 이름, 시작 beat, 끝 beat, 적용 강도, 트랙별 1~5 우선순위를 가진다.
- 왼쪽 트랙 목록 아래 AutoMix 영역에 `우선순위` 패널을 추가했다. 사용자는 `＋ 구간`으로 구간을 만들고 시작/끝 마디, 적용 강도, 트랙별 우선순위를 수정할 수 있다.
- AutoMix 실행 시 기존 RMS/밀도 기반 볼륨 계산에 구간별 우선순위 가중치를 곱한다. 우선순위 3은 기본, 1은 뒤로, 5는 중심으로 처리한다.
- 구간과 겹치는 음표 길이를 기준으로 해당 구간의 우선순위가 트랙 볼륨에 반영된다. 음표가 없는 트랙은 기존처럼 건드리지 않는다.
- 용어 단순화 편집용 Excel 파일을 `outputs/terminology/beginner-music-terminology.xlsx`에 만들었다. 사용자는 `beginnerTerm`, `easyExplanation`, `memo` 열을 수정해 앱 용어 정리 기준으로 쓸 수 있다.

## 2026-04-19 AutoMix 단순화와 스크롤 고정 보정

- 사용자가 수정한 용어 Excel을 확인했다. `beginnerTerm` 열에서 `Velocity`, `Panpot`, `Event`, `BPM`, `Tempo`, `Arrange`, `Open` 등 일부 항목이 비어 있는 상태였다. 파일은 사용자 수정본을 보존하기 위해 건드리지 않았다.
- AutoMix 우선순위 UI가 복잡해 보여서 트랙별 1~5 목록 대신 `믹스 컷` 카드 방식으로 단순화했다. 각 컷은 시작/끝, 강도, `이 구간 중심 트랙`만 고르면 된다.
- 내부 데이터는 기존 `autoMixSections.priorities`를 유지하되, 중심 트랙은 우선순위 5, 나머지는 악기별 기본 우선순위로 자동 설정한다.
- AutoMix 계산이 1.0으로 쉽게 클램프되어 변화가 약해 보이던 문제를 줄이기 위해 트랙별 raw mix 값을 먼저 계산하고, 그중 최대값 기준으로 상대 볼륨을 다시 정규화하도록 바꿨다.
- 재생 중 AutoMix를 눌러도 기존 예약 음표에는 볼륨 변화가 바로 적용되지 않던 문제를 줄이기 위해, 재생 중 AutoMix 실행 시 현재 위치에서 재생을 다시 시작해 새 볼륨이 반영되게 했다.
- `html`, `body`, `#root`와 `editor-shell`을 고정 높이/overflow hidden으로 바꾸고, 트랙 목록만 내부 스크롤되게 보정했다. 사이트 전체가 아래로 스크롤되는 흐름을 막는 목적이다.

## 2026-04-19 한국어 전용 UI와 AutoMix 실제 적용 보정

- 상단 메뉴, 파일/편집 메뉴, 악기 선택창, 트랙 버튼, 하단 세부 편집 탭, 배치/빠르기 화면의 보이는 영어 문구를 한국어로 바꿨다.
- General MIDI 악기 표시명 128개를 한국어 이름으로 매핑했다. SoundFont 로딩용 원본 이름은 내부 로딩 키로만 유지하고, 화면에는 한국어 악기명이 보이게 했다.
- 드럼 행 이름과 파워 드럼 세트 이름도 한국어 중심으로 바꿨다.
- 용어 Excel 생성 파일을 다시 만들었다. 새 `outputs/terminology/beginner-music-terminology.xlsx`는 영어 원어 열을 제거하고 `구역`, `현재 표시`, `초급자 표현`, `쉬운 설명`, `중요도`, `메모`만 둔다.
- AutoMix가 트랙 볼륨만 바꾸는 수준이라 구간별 효과가 약했던 문제를 줄이기 위해, 믹스 컷과 겹치는 음표의 `volume` 값도 중심 트랙 우선순위에 따라 직접 조절하게 했다.
- 이번 수정 후 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다.

## 2026-04-19 AutoMix 즉시 적용, 높은 음 편집, 하단 탭 정리

- 사용자가 지적한 대로 용어 Excel은 사용자가 준 파일의 번역값을 기준으로 반영했어야 했는데, 이전 작업에서 새 파일로 덮어쓴 실수가 있었다. 이후에는 사용자가 채운 파일을 덮어쓰지 않고 읽어서 반영해야 한다.
- AutoMix 버튼만 눌렀을 때 컷이 없으면 아무 일도 없는 것처럼 보이던 문제를 고쳤다. 이제 컷이 없으면 자동으로 `전체 곡` 컷을 임시 적용해 트랙 볼륨과 음표 `volume`을 바로 조절한다.
- 높은 음 MIDI가 들리지만 피아노 롤에 행이 없어 편집할 수 없던 문제를 고쳤다. 선택 트랙의 음표 최저/최고 pitch에 맞춰 피아노 롤 행 범위가 자동 확장된다.
- 하단 세부 편집 탭에서 `소리 세기` 아래에 또 `소리 세기`가 보이던 중복 서브 설명을 제거했다.
- 이번 수정 후 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다.

## 최신 압축 문맥

- 현재 목표는 Signal MIDI와 비슷한 어두운 한국어 피아노 롤 작곡 편집기다.
- 사용자가 직접 해야 하는 작업은 서버 실행과 배포다. Codex는 개발 서버를 직접 켜지 않고 `npm.cmd run dev` 안내만 한다.
- 주요 파일은 `src/App.tsx`, `src/App.css`, `src/lib/audio/toneTransport.ts`, `src/lib/audio/exportMp3.ts`, `src/lib/midi/importMidi.ts`, `src/lib/midi/exportMidi.ts`, `src/types/music.ts`다.
- 현재 핵심 기능은 피아노 롤 입력/이동/리사이즈/우클릭 삭제/드래그 삭제, 박스 선택, 자유 선택, 선택 박스 이동, 패턴 반복, 키보드 실시간 입력, JSON/MIDI 불러오기, JSON/MIDI/MP3 저장, AutoMix 1차, Undo/Redo, 트랙 채널, 하단 Velocity/Volume/Pan/Pitch Bend/Expression/Modulation/Event 편집이다.
- 드럼은 행 이름과 소리 계열이 분리되어 있지만 아직 실제 샘플 기반 SoundFont 드럼은 아니다. 다음 큰 개선은 SF2/GS 드럼 은행을 로컬 샘플로 변환해 GM drum note별 샘플을 연결하는 작업이다.
- 반복해서 검증할 명령은 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`다.

## 향후 2차 개발 메모

- 용어 설명: Velocity는 소리 세기, Pitch는 음높이, Volume은 음량처럼 쉽게 설명한다.
- 자동 믹스: 악기 간 음량 균형을 초급자가 이해하기 쉬운 방식으로 자동 조절한다.
- Firebase 저장: 1차 이후 계정 기반 프로젝트 목록 저장으로 확장한다.
- 오디오 출력: 초기에는 가능한 방식으로 다운로드하고, 이후 MP3/WAV 렌더링을 개선한다.
- 악기 소리: 현재 Tone.js 합성음 기반이다. 더 실제 악기에 가까운 소리는 추후 SoundFont 또는 샘플 기반 재생으로 바꾸는 것이 좋다.
- Signal MIDI 세부 UI: Arrange, Tempo, Help, Settings, Sign In 등은 현재 자리만 남겨두고 추후 연결한다.

## 유용한 명령

```powershell
npm.cmd run dev
npx.cmd tsc -b tsconfig.app.json
npm.cmd run lint
npm.cmd run build
```

주의: 이 환경에서는 PowerShell 실행 정책 때문에 `npm` 대신 `npm.cmd`를 사용한다.

## 2026-04-19 사용자 제공 SF2 드럼킷 적용

- 사용자가 제공한 `C:/Users/MINJAE/Downloads/Standard_Dum_Kit__by_Charlie.SF2` 파일을 앱에서 직접 접근할 수 있도록 `public/soundfonts/Standard_Dum_Kit__by_Charlie.SF2`로 복사했다.
- `src/lib/audio/sf2DrumKit.ts`를 추가해 브라우저에서 SF2의 RIFF/sdta/pdta 구조를 직접 읽고, bank 128 드럼 프리셋의 GM 드럼 키별 샘플을 찾아 재생하도록 했다.
- 드럼 트랙(`instrumentId === 'drums'`)은 이제 SF2 샘플 드럼을 우선 사용하고, SF2 로딩 실패 또는 해당 키 샘플이 없을 때만 기존 Tone.js 합성 드럼 fallback을 사용한다.
- 이 SF2는 Tone.Sampler의 mp3 SoundFont가 아니라 원본 `.SF2` 바이너리이므로, 샘플은 Web Audio `AudioBufferSourceNode`로 직접 재생한다. 트랙 볼륨/노트 볼륨은 기존 재생 경로에서 velocity로 전달된 값을 따른다.
- 검증 명령 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다. 빌드에서는 기존처럼 Vite 청크 크기 경고만 남는다.

## 2026-04-19 드럼 SF2 실제 적용 경로 수정, 오디오 녹음/업로드, AutoMix 잠금

- 드럼이 기존과 똑같이 들리던 원인은 재생 스케줄러가 드럼 악기에도 MIDI 번호가 아니라 주파수 값을 넘기던 문제였다. `BeginnerInstrument.expectsMidi`를 추가하고, 드럼/SF2 드럼킷은 GM drum note 번호를 그대로 받도록 고쳤다.
- 피아노/브라스 같은 일반 악기는 기존처럼 주파수로 재생하고, 드럼만 MIDI 번호로 재생한다. 이 변경은 실시간 재생, 미리듣기, 원샷 프리뷰, 윈도우 스케줄러에 모두 적용했다.
- 패턴 반복 핸들을 오른쪽으로 늘릴 때 복제 패턴 사이에 0.25 beat 간격을 두도록 했다. 반복 선택 박스의 미리보기 폭도 이 간격을 포함해 늘어난다.
- 자동 믹스 실행 중에는 `자동 믹스 중` 오버레이를 표시하고, 사용자가 다른 작업을 하지 못하게 화면을 덮는다. 자동 믹스 버튼도 실행 중 비활성화된다.
- 상단에 `믹스 우선순위` 탭을 추가했다. 이 탭에서 컷을 추가하고 구간별 중심 트랙, 시작/끝 마디, 강도를 설정할 수 있다. 우선순위가 없으면 기존처럼 기본 우선순위를 사용한다.
- 프로젝트에 `audioClips`를 추가했다. 선택 트랙에 오디오 파일을 업로드하거나 마이크로 음성 녹음한 뒤 클립으로 배치할 수 있다.
- 오디오 클립은 배치 탭에서 초록색 블록으로 보인다. 재생 시 선택 트랙 위치에 맞춰 함께 재생되고, MP3 출력에도 포함된다.
- 오디오 클립이 하나라도 있는 프로젝트는 MIDI 저장 시 경고를 띄우고 저장하지 않는다. MIDI는 녹음/오디오 파일을 담을 수 없으므로 MP3 저장을 사용해야 한다.
- 검증 명령 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다. 빌드에서는 기존처럼 Vite 청크 크기 경고만 남는다.

## 2026-04-19 드럼 SF2 sampleRate 보정

- 사용자가 드럼 소리가 계속 이상하다고 지적했다. 확인 결과 `Standard_Dum_Kit__by_Charlie.SF2` 파일 자체는 public 복사본과 다운로드 원본의 SHA256 해시가 동일했다.
- 실제 문제는 SF2의 일부 `shdr.sampleRate` 값이 수십만~수백만 Hz처럼 비정상적으로 들어가 있고, 브라우저 파서가 그 값을 그대로 믿어 스네어/하이햇/심벌 샘플을 0.001~0.02초짜리 소리처럼 압축 재생하던 것이었다.
- `src/lib/audio/sf2DrumKit.ts`에서 드럼 샘플 AudioBuffer 생성 시 sampleRate를 `44100`으로 고정했다. 드럼 샘플은 피치 변형 없이 `playbackRate = 1`로 재생한다.
- 첫 재생 때 SF2 로딩이 끝나기 전에 기존 합성 드럼 fallback으로 새지 않도록 드럼 악기 전용 `readyTimeoutMs`를 추가했고, `waitForInstrumentReady`가 악기별 대기 시간을 읽도록 했다.
- 검증 명령 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다. 빌드에서는 기존처럼 Vite 청크 크기 경고만 남는다.

## 2026-04-19 드럼 키 범위와 타악기 종류 확장

- 사용자가 드럼 종류가 snare/tom 정도만 있는 것이 아니라고 지적했다. 드럼 트랙 피아노 롤 범위를 기존 GM 35~81에서 SF2 매핑에 맞춘 26~87로 확장했다.
- `DRUM_LABELS`에 스냅, 큐, 슬랩, 스크래치, 메트로놈, 하이햇/심벌, 봉고/콩가/팀발레스/아고고, 카바사/마라카스, 휘슬/귀로, 우드블록/쿠이카/트라이앵글, 셰이커/징글벨/벨트리/캐스터네츠/수르도까지 표시명을 추가했다.
- 드럼 트랙에서는 음표 블록 안에 `C5` 같은 피치명이 아니라 해당 타악기 이름이 보이도록 했다.
- 드럼 트랙의 왼쪽 키 영역을 피아노 건반 흑백 UI가 아니라 드럼 패드 목록처럼 보이게 `is-drum` 스타일을 추가했다.
- 키보드 실시간 입력 드럼 매핑에도 추가 타악기 일부를 포함했다.
- 검증 명령 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다. 빌드에서는 기존처럼 Vite 청크 크기 경고만 남는다.

## 2026-04-19 AutoMix 체감 보강과 결과 표시

- 사용자가 자동 믹스가 무엇을 바꾸는지 모르겠고 차이가 약하다고 지적했다.
- AutoMix 계산을 더 명확하게 바꿨다. 이제 트랙을 `리듬 중심`, `저음 받침`, `주요 선율`, `리듬 악기`, `배경 선율`, `보조 선율`, `신스 선율`, `공간 배경`, `중심 악기` 역할로 분류한다.
- 역할과 우선순위, 음표 밀도, RMS 기반으로 트랙 전체 `volume`을 더 분명하게 조정한다. 이전처럼 상대 정규화로 대부분 비슷하게 붙는 문제를 줄였다.
- 트랙별 `pan` 값을 추가했다. 드럼/베이스/주요 선율은 가운데에 두고, 배경/리듬/보조 악기는 좌우로 나누는 기본 값을 적용한다. 음표별 `pan`에도 일부 반영된다.
- AutoMix가 구간 우선순위와 겹치는 음표의 `volume`을 조정하고, 몇 개 음표가 보정됐는지 결과로 보여준다.
- 왼쪽 트랙 목록에 현재 음량 막대와 좌우 위치 요약을 표시한다.
- AutoMix 우선순위 패널과 `믹스 우선순위` 탭에 AutoMix 결과 리포트를 표시한다. 각 트랙의 역할, 음량 변경 전/후, 좌우 위치, 보정된 음표 수를 확인할 수 있다.
- 검증 명령 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다. 빌드에서는 기존처럼 Vite 청크 크기 경고만 남는다.
## 2026-04-20 락 드럼 SoundFont 적용

- 사용자가 드럼 소리가 뿅뿅거리지 않고 락 드럼 스타일이어야 한다고 요청했다.
- Zanderjaz 무료 드럼 SoundFont 목록에서 `Drum Set JD Rockset 5.sf2`와 `TamaRockSTAR.sf2`를 확인하고 `public/soundfonts`에 내려받았다.
- `src/lib/audio/sf2DrumKit.ts`는 이제 `DrumSetJDRockset5.sf2`를 1순위, `TamaRockSTAR.sf2`를 2순위로 시도한다. 실패하면 `Elementic.sf2`, `Standard_Dum_Kit__by_Charlie.SF2` 순서로 fallback한다.
- SF2 `shdr` 파싱에서 `sampleRate`, `originalPitch`, `pitchCorrection` 오프셋이 잘못 읽히던 문제를 고쳤다. 이 오류는 샘플 재생 기준을 흐트러뜨려 드럼 파일을 바꿔도 이상한 소리가 날 수 있는 원인이었다.
- 드럼 프리셋 선택은 bank 128을 우선하되, preset 이름에 `rock`, `tama`, `jd`, `drum`, `kit`이 들어간 것을 더 선호하도록 바꿨다.
- 샘플별 AudioBuffer 캐시는 SoundFont 파일 경로와 샘플 위치까지 포함해 구분한다. 여러 SF2를 순차 fallback해도 샘플 캐시가 섞이지 않게 하기 위한 조치다.
- 검증 명령 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다. 빌드에서는 기존처럼 Vite chunk 크기 경고만 남는다.

## 2026-04-20 드럼 최소 울림 시간 보정

- 사용자가 드럼 소리가 멜로디 길이에 너무 의존해 딱딱 끊긴다고 지적했다.
- `src/App.tsx`의 실시간 재생 스케줄러에서 드럼 트랙을 일반 멜로디 악기와 분리했다. 드럼은 `triggerAttack` 후 짧은 노트 길이에 맞춰 바로 `triggerRelease`하지 않고, 원샷 히트처럼 `triggerAttackRelease`로 재생한다.
- 킥, 스네어, 하이햇, 탐, 심벌, 퍼커션별 최소 재생 시간을 두었다. 아주 짧은 음표라도 킥/스네어/심벌 tail이 어느 정도 남도록 하기 위한 조치다.
- `src/lib/audio/sf2DrumKit.ts`의 SF2 드럼 샘플도 release 요청이 너무 빨리 들어오면 최소 울림 시간이 지난 뒤에 끊도록 했다. 단, 정지/dispose는 작업자가 의도한 중단이므로 강제로 끊는다.
- 검증 명령 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다. 빌드에서는 기존처럼 Vite chunk 크기 경고만 남는다.

## 2026-04-20 반복 음표 어택과 트랙 음량 조절

- 사용자가 같은 피치의 음표가 맞닿아 있을 때 한 번만 들리는 문제를 지적했다. 드럼이 아닌 악기는 같은 음이 이어져도 각 음표 시작마다 다시 어택되어야 한다.
- `src/App.tsx`의 실시간 재생 스케줄러에서 비드럼 트랙도 각 음표마다 `triggerAttackRelease`를 호출하도록 바꿨다. 기존 active count 방식은 같은 피치가 이미 울리고 있으면 새 어택을 생략했기 때문에 이어진 음표가 하나처럼 들렸다.
- 드럼 행 이름을 한국어 사용자에게 더 쉽게 바꿨다. 예: `킥 드럼(쿵)`, `스네어(탁)`, `닫힌 하이햇(짧게 칙)`, `크래시 심벌(쾅)`.
- 왼쪽 트랙 카드에 악기별 음량 슬라이더를 추가했다. 사용자는 트랙별 음량을 0~120 범위에서 직접 조절할 수 있고, 기존 음량 막대는 현재 상태 표시로 유지한다.
- 검증 명령 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다. 빌드에서는 기존처럼 Vite chunk 크기 경고만 남는다.

## 2026-04-20 Standard Drum Kit 추가

- 사용자가 현재 락 성향의 드럼은 좋지만 약한 드럼도 하나 더 필요하다고 요청했다.
- `src/App.tsx`의 악기 목록에서 Drums 카테고리에 `Power Drum Kit` 다음 `Standard Drum Kit`을 추가했다.
- `src/lib/audio/sf2DrumKit.ts`는 드럼 키트 스타일을 `power`와 `standard`로 분리한다. `power`는 `DrumSetJDRockset5.sf2` → `TamaRockSTAR.sf2` 순서를 우선하고, `standard`는 `Standard_Dum_Kit__by_Charlie.SF2`를 우선한다.
- `src/lib/audio/toneTransport.ts`에 `isDrumInstrument`를 추가해 `drums`, `standard-drums` 모두 드럼 트랙으로 처리한다.
- `src/lib/midi/generalMidi.ts`는 `standard-drums` 라벨/아이콘/이미지를 처리하고, `src/lib/midi/exportMidi.ts`는 `standard-drums`도 GM 드럼 채널로 저장한다.
- 검증 명령 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다. 빌드에서는 기존처럼 Vite chunk 크기 경고만 남는다.

## 2026-04-20 키보드 실시간 입력 정확도 개선

- 사용자가 키보드 입력으로 실시간 멜로디를 찍을 때 정확도가 떨어진다고 지적했다.
- `src/App.tsx`의 키보드 입력 핸들러가 `keydown`/`keyup`의 `event.timeStamp`를 `startKeyboardNote`/`finishKeyboardNote`로 넘기도록 바꿨다.
- 입력 시작/끝 beat는 이벤트가 실제 발생한 시간 기준으로 계산한다. 핸들러 실행 시점이 조금 밀려도 기록 위치가 덜 밀린다.
- 기존처럼 현재 음표 단위 grid에 강하게 snap하지 않고, 1/64 beat 단위로만 가볍게 quantize한다. 실시간 연주 타이밍을 더 보존하기 위한 조치다.
- 재생 중인 선택 트랙 악기가 이미 준비되어 있으면 별도 preview 악기를 만들지 않고 active playback instrument로 바로 소리를 낸다. 드럼은 원샷 hit, 비드럼은 keydown에서 attack 후 keyup에서 release한다.
- 검증 명령 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다. 빌드에서는 기존처럼 Vite chunk 크기 경고만 남는다.

## 2026-04-20 오디오 파일 트랙과 MP3 출력 보정

- 사용자가 파일로 악기를 업로드하면 별도 트랙이 추가되고, 그 트랙은 멜로디가 아니라 주파수/파형처럼 보여야 한다고 요청했다.
- `src/types/music.ts`에 `Track.kind?: 'instrument' | 'audio'`와 `AudioClip.waveform?: number[]`를 추가했다.
- 오디오 파일 업로드와 드래그 앤 드롭은 이제 파일마다 새 `오디오 트랙`을 만든다. 오디오 트랙은 `instrumentId: audio-track`, `kind: audio`이며 음표 배열은 비어 있다.
- 오디오 파일을 읽을 때 Web Audio로 파형 peak 배열을 계산해 `AudioClip.waveform`에 저장한다.
- 오디오 트랙을 선택하면 피아노롤 대신 `audio-track-editor`가 뜬다. 파형을 위아래로 드래그하면 해당 오디오 클립의 `volume`이 0~150 범위에서 조절된다.
- 배치 화면의 오디오 클립은 단순 블록 대신 파형 막대를 함께 보여준다.
- 실시간 재생에서 오디오 클립은 HTMLAudioElement 대신 Web Audio `AudioBufferSourceNode + Gain + StereoPanner`로 재생한다. 트랙/클립 볼륨과 좌우 위치가 MP3 출력과 더 가깝게 적용된다.
- `src/lib/audio/exportMp3.ts`는 강한 compressor를 제거하고 master gain 중심으로 바꿨다. 오디오 클립 gain 범위와 드럼 최소 울림 시간도 편집기 재생과 맞췄다.
- 주의: MIDI/일반 악기 MP3 출력은 아직 브라우저 실시간 SoundFont 샘플과 완전히 동일하지는 않다. 이번 변경은 오디오 클립, 드럼 지속 시간, 마스터 압축 차이를 줄이는 1차 보정이다.
- 검증 명령 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다. 빌드에서는 기존처럼 Vite chunk 크기 경고만 남는다.

## 2026-04-20 선택 박스 위치와 패턴 반복 안정화

- 사용자가 박스 선택 후 화면 크기나 줌이 바뀌면 선택 박스가 음표 위치와 어긋나고, 패턴 반복 핸들을 늘릴 때 반복 개수가 갑자기 튄다고 지적했다.
- `src/App.tsx`의 선택 박스 계산은 이제 캐시된 픽셀 geometry만 의존하지 않고 현재 피아노롤 폭과 선택된 음표의 beat/pitch 위치로 다시 계산한다.
- 선택 음표, 피아노롤 줌, 음표 단위, 피치 행, 총 step 수가 바뀌면 선택 박스 위치와 크기를 재계산하는 effect를 추가했다.
- 패턴 반복 preview는 `Math.round` 방식에서 드래그 거리 기반 `Math.floor` 방식으로 바꿨다. 패턴 한 단위만큼 더 드래그해야 다음 반복 수가 늘어나므로, 중간 임계점에서 두 개씩 튀는 느낌을 줄인다.
- 검증 명령 `npx.cmd tsc -b tsconfig.app.json`, `npm.cmd run lint`, `npm.cmd run build`가 통과했다. 빌드에서는 기존처럼 Vite chunk 크기 경고만 남는다.
