// @ts-nocheck
import { jsx as _jsx } from "react/jsx-runtime";import { useEffect, useMemo, useRef, useState, } from 'react';import './App.css';import { expandProjectForArrangement } from './lib/arrangement/trackArrangement';import { isDrumInstrument } from './lib/audio/toneTransport';import { BEATS_PER_BAR, DEFAULT_BEAT_WIDTH, DEFAULT_PROJECT_LENGTH_BEATS, DRUM_LABELS, DRUM_PITCHES, EDITING_TAIL_BEATS, FLOAT_EPSILON, INSTRUMENT_CATEGORY_ORDER, INSTRUMENT_OPTIONS, KEY_COLUMN_WIDTH, MIN_DURATION_BEATS, PLAYHEAD_AUTO_SCROLL_THROTTLE_MS, PLAYHEAD_SCROLL_PADDING, ROLL_HEADER_HEIGHT, ROLL_ROW_HEIGHT, TEMPO_GRAPH_MAX, TEMPO_GRAPH_MIN, } from './features/editor/constants';import { buildTempoTimeline, createId, getBeatAtSecondsFromTimeline, getDynamicPitches, getGroupedPitchStep, getNextZoom, getNormalizedTempoSections, getNotesEndBeat, getPatternRepeatGroupNoteIds, getPitchName, getVisibleBars, nearlyEqual, normalizePatternRepeatGapBeats, prunePatternRepeatGroups, readSavedProject, } from './features/editor/helpers';import { toStoredNote } from './features/editor/utils/noteUtils';import { useProjectStorage } from './features/editor/hooks/useProjectStorage';import { useProjectHistory } from './features/editor/hooks/useProjectHistory';import { useTrackActions } from './features/editor/hooks/useTrackActions';import { useTempoActions } from './features/editor/hooks/useTempoActions';import { useAutoMixActions } from './features/editor/hooks/useAutoMixActions';import { useArrangementActions } from './features/editor/hooks/useArrangementActions';import { useKeyboardRecording } from './features/editor/hooks/useKeyboardRecording';import { usePreviewNote } from './features/editor/hooks/usePreviewNote';import { usePianoRollSelection } from './features/editor/hooks/usePianoRollSelection';import { usePianoRollDrag } from './features/editor/hooks/usePianoRollDrag';import { usePlayback } from './features/editor/hooks/usePlayback';import { useFileActions } from './features/editor/hooks/useFileActions';import { useDialogState } from './features/editor/hooks/useDialogState';import { useEditorDerived } from './features/editor/hooks/useEditorDerived';import { useEditorState } from './features/editor/hooks/useEditorState';import { useMenuState } from './features/editor/hooks/useMenuState';import { usePlaybackRefs } from './features/editor/hooks/usePlaybackRefs';import { useKeyboardShortcuts } from './features/editor/hooks/useKeyboardShortcuts';import { useNoteEditing } from './features/editor/hooks/useNoteEditing';import { usePatternEditing } from './features/editor/hooks/usePatternEditing';import { usePlaybackScroll } from './features/editor/hooks/usePlaybackScroll';import { usePointerInteractions } from './features/editor/hooks/usePointerInteractions';import { useSelectionTools } from './features/editor/hooks/useSelectionTools';import { EditorLayout } from './features/editor/components/EditorLayout';import { AutoMixCutOverlay } from './features/editor/components/AutoMixCutOverlay';import { TempoSectionOverlay } from './features/editor/components/TempoSectionOverlay';import { clampNoteControlValue, getNoteControlValue, getSelectedNoteValue as getSelectedNoteValueFromNotes, quantizeValue, } from './features/editor/utils/noteControlUtils';export function EditorApp() {    const [project, setProjectState] = useState(() => readSavedProject());    const { activeDetailTerm, activeEditorTab, allTrackMelodyMode, autoMixGenrePreset, autoMixPanelOpen, autoMixReport, detailPanelOpen, draggingNoteId, isAutoMixing, isDraggingFile, isExportingMp3, isPlaying, isRecordingVoice, keyboardInputEnabled, lassoPoints, noteDivision, patternRepeatGapInput, playbackBeat, pressedPitch, resizingNoteId, rollZoom, selectedAutoMixSectionId, selectedNoteIds, selectedTempoSectionId, selectedTrackPlacementId, selectionBox, setActiveDetailTerm, setActiveEditorTab, setAllTrackMelodyMode, setAutoMixGenrePreset, setAutoMixPanelOpen, setAutoMixReport, setDetailPanelOpen, setDraggingNoteId, setHistoryVersion, setIsAutoMixing, setIsDraggingFile, setIsExportingMp3, setIsPlaying, setIsRecordingVoice, setKeyboardInputEnabled, setLassoPoints, setNoteDivision, setPatternRepeatGapInput, setPlaybackBeat, setPressedPitch, setResizingNoteId, setRollZoom, setSelectedAutoMixSectionId, setSelectedNoteIds, setSelectedTempoSectionId, setSelectedTrackPlacementId, setSelectionBox, setTempoInput, setToolMode, tempoInput, toolMode, } = useEditorState(project);    const { closePianoRollContextMenu, closeTrackContextMenu, editMenuOpen, fileMenuOpen, pianoRollContextMenu, setEditMenuOpen, setFileMenuOpen, setPianoRollContextMenu, setTrackContextMenu, trackContextMenu, } = useMenuState();    const { instrumentCategory, instrumentMenuTrackId, pendingInstrumentId, setInstrumentCategory, setInstrumentMenuTrackId, setPendingInstrumentId, } = useDialogState();    const { activeAudioElementsRef, activeAudioNodesRef, activeEditorTabRef, activeInstrumentsRef, activeIntervalsRef, activePlaybackTracksRef, activeTimeoutsRef, audioFileInputRef, autoMixPanelOpenRef, detailGraphDragRef, detailGraphSvgRef, eraseRef, fileInputRef, heldPreviewRef, interactionHandlersRef, keyPreviewRef, keyboardRecordingRef, lastNoteDurationRef, lastPlayheadAutoScrollAtRef, lassoSelectionRef, mediaRecorderRef, noteDragRef, otherNotesByPitchCacheRef, patternClipboardRef, patternRepeatRef, patternSelectionRef, pendingPointerMoveRef, pianoRollRef, playbackBeatRef, playbackPressedPitchCountsRef, playbackSessionRef, playbackStartBeatRef, playbackStartMsRef, playbackStartSecondsRef, playbackTempoTimelineRef, pointerMoveFrameRef, previewTokenRef, recordingChunksRef, recordingStartBeatRef, recordingStartMsRef, rightClickRollActionRef, rightEraseRef, rollPointerGeometryRef, totalBeatsRef, trackPlacementDragRef, } = usePlaybackRefs();    const projectRef = useRef(project);    lastNoteDurationRef.current = Math.max(lastNoteDurationRef.current, MIN_DURATION_BEATS * 2);    const { beginHistoryBatch, canRedo, canUndo, endHistoryBatch, redoProject, resetProjectHistory, restoreProject, setProject, undoProject, } = useProjectHistory({        projectRef,        setProjectState,        setHistoryVersion,        setSelectedNoteIds,        setSelectionBox,        patternSelectionRef,    });    useProjectStorage({        project,        projectRef,        resizingNoteId,        noteDragRef,        eraseRef,        rightEraseRef,    });    const erasedNoteIdsRef = useRef(new Set());    const rollPointerCaptureRef = useRef(null);    const selectedTrack = useMemo(() => project.tracks.find((track) => track.id === project.selectedTrackId) ?? project.tracks[0], [project.selectedTrackId, project.tracks]);    const selectedTrackNotes = useMemo(() => (selectedTrack ? project.notesByTrack[selectedTrack.id] ?? [] : []), [project.notesByTrack, selectedTrack]);    const selectedAudioClips = useMemo(() => (selectedTrack ? (project.audioClips ?? []).filter((clip) => clip.trackId === selectedTrack.id) : []), [project.audioClips, selectedTrack]);    const selectedTrackIsAudio = selectedTrack?.kind === 'audio' || selectedTrack?.instrumentId === 'audio-track';    const allTrackNotes = useMemo(() => project.tracks.flatMap((track) => ((project.notesByTrack[track.id] ?? []).map((note) => ({ ...note, trackId: track.id })))), [project.notesByTrack, project.tracks]);    const autoMixSections = useMemo(() => project.autoMixSections ?? [], [project.autoMixSections]);    const selectedAutoMixSection = autoMixSections.find((section) => section.id === selectedAutoMixSectionId) ?? null;    const selectedNote = useMemo(() => (allTrackMelodyMode ? allTrackNotes : selectedTrackNotes.map((note) => ({ ...note, trackId: selectedTrack?.id ?? '' })))        .find((note) => note.id === project.selectedNoteId) ?? null, [allTrackMelodyMode, allTrackNotes, project.selectedNoteId, selectedTrack?.id, selectedTrackNotes]);    const selectedNoteIdSet = useMemo(() => new Set(selectedNoteIds), [selectedNoteIds]);    const selectedPatternNotes = useMemo(() => (allTrackMelodyMode ? allTrackNotes : selectedTrackNotes.map((note) => ({ ...note, trackId: selectedTrack?.id ?? '' })))        .filter((note) => selectedNoteIdSet.has(note.id)), [allTrackMelodyMode, allTrackNotes, selectedNoteIdSet, selectedTrack?.id, selectedTrackNotes]);    const allTrackNotesById = useMemo(() => new Map(allTrackNotes.map((note) => [note.id, note])), [allTrackNotes]);    const allTrackNoteIdSet = useMemo(() => new Set(allTrackNotes.map((note) => note.id)), [allTrackNotes]);    const patternRepeatGroupByNoteId = useMemo(() => {        const groupByNoteId = new Map();        (project.patternRepeatGroups ?? []).forEach((group) => {            getPatternRepeatGroupNoteIds(group).forEach((noteId) => {                groupByNoteId.set(noteId, group);            });        });        return groupByNoteId;    }, [project.patternRepeatGroups]);    const selectedPatternRepeatGroup = useMemo(() => {        if (selectedNoteIds.length === 0)            return null;        const selectedIds = new Set(selectedNoteIds);        return (project.patternRepeatGroups ?? []).find((group) => {            const groupNoteIds = getPatternRepeatGroupNoteIds(group);            return groupNoteIds.length === selectedIds.size && groupNoteIds.every((noteId) => selectedIds.has(noteId));        }) ?? null;    }, [project.patternRepeatGroups, selectedNoteIds]);    useEffect(() => {        if (!selectedPatternRepeatGroup)            return;        const cellsPerBeat = noteDivision / 4;        const gapSteps = Math.max(0, Math.round(selectedPatternRepeatGroup.gapBeats * cellsPerBeat));        setPatternRepeatGapInput(String(gapSteps));    }, [noteDivision, selectedPatternRepeatGroup]);    const editableSelectedNotes = useMemo(() => (selectedPatternNotes.length > 0        ? selectedPatternNotes        : selectedNote            ? [selectedNote]            : []), [selectedNote, selectedPatternNotes]);    const sortedEditableSelectedNotes = useMemo(() => [...editableSelectedNotes].sort((left, right) => (left.startBeat - right.startBeat || right.pitch - left.pitch)), [editableSelectedNotes]);    const arrangedProject = useMemo(() => (activeEditorTab === 'arrange' ? expandProjectForArrangement(project) : project), [activeEditorTab, project]);    const projectEndBeat = useMemo(() => {        const notesEndBeat = getNotesEndBeat(project.notesByTrack);        const clipsEndBeat = (project.audioClips ?? []).reduce((latestEnd, clip) => Math.max(latestEnd, clip.startBeat + clip.durationBeats), 0);        const placementsEndBeat = (project.patternPlacements ?? []).reduce((latestEnd, placement) => Math.max(latestEnd, placement.startBeat + placement.spanBeats), 0);        return Math.max(notesEndBeat, clipsEndBeat, placementsEndBeat);    }, [project.audioClips, project.notesByTrack, project.patternPlacements]);    const projectLengthBeats = Math.max(DEFAULT_PROJECT_LENGTH_BEATS, projectEndBeat + EDITING_TAIL_BEATS);    const visibleBars = getVisibleBars(projectLengthBeats);    const totalBeats = visibleBars * BEATS_PER_BAR;    const tempoSections = useMemo(() => getNormalizedTempoSections(project, totalBeats), [project, totalBeats]);    const stepsPerBeat = noteDivision / 4;    const defaultDurationBeats = Math.max(MIN_DURATION_BEATS, Math.round(lastNoteDurationRef.current * stepsPerBeat) / stepsPerBeat);    const totalSteps = totalBeats * stepsPerBeat;    const rollPitches = useMemo(() => {        if (!allTrackMelodyMode && selectedTrack?.instrumentId && isDrumInstrument(selectedTrack.instrumentId)) {            return DRUM_PITCHES;        }        return getDynamicPitches(allTrackMelodyMode ? allTrackNotes : selectedTrackNotes);    }, [allTrackMelodyMode, allTrackNotes, selectedTrack?.instrumentId, selectedTrackNotes]);    const selectedTempoSection = useMemo(() => tempoSections.find((section) => section.id === selectedTempoSectionId) ?? null, [selectedTempoSectionId, tempoSections]);    const { getCurrentPlaybackBeat, getMinimumPlaybackDrumSeconds, resetPlayback, startPlaybackAt, togglePlayback, } = usePlayback({        activeAudioElementsRef,        activeAudioNodesRef,        activeInstrumentsRef,        activeIntervalsRef,        activePlaybackTracksRef,        activeTimeoutsRef,        heldPreviewRef,        isPlaying,        keyboardRecordingRef,        keyPreviewRef,        lastPlayheadAutoScrollAtRef,        pianoRollRef,        playbackBeatRef,        playbackPressedPitchCountsRef,        playbackSessionRef,        playbackStartBeatRef,        playbackStartMsRef,        playbackStartSecondsRef,        playbackTempoTimelineRef,        projectRef,        setIsPlaying,        setPlaybackBeat,        setPlaybackPosition,        setPressedPitch,        totalBeats,        totalBeatsRef,    });    const { addTempoSection, changeTempoInput, commitTempoInput, deleteTempoSection, focusTempoSection, updateTempo, updateTempoSection, } = useTempoActions({        getCurrentPlaybackBeat,        project,        projectRef,        selectedTempoSectionId,        setProject,        setSelectedTempoSectionId,        setTempoInput,        tempoInput,        tempoSections,        totalBeats,    });    const beatWidth = DEFAULT_BEAT_WIDTH * rollZoom;    const stepWidth = beatWidth / stepsPerBeat;    const rollTimelineStyle = { gridTemplateColumns: `repeat(${visibleBars}, minmax(64px, 1fr))` };    const rollSurfaceStyle = {        '--bar-width': `${beatWidth * BEATS_PER_BAR}px`,        '--beat-width': `${beatWidth}px`,        '--roll-grid-height': `${rollPitches.length * ROLL_ROW_HEIGHT}px`,        '--roll-grid-width': `${totalBeats * beatWidth}px`,        '--step-width': `${stepWidth}px`,        '--total-steps': totalSteps,        '--visible-bars': visibleBars,    };    const rollShellStyle = {        ...rollSurfaceStyle,        gridTemplateColumns: `64px minmax(${totalBeats * beatWidth}px, 1fr)`,    };    const autoMixMarkerSections = useMemo(() => autoMixSections        .filter((section) => section.endBeat > 0 && section.startBeat < totalBeats)        .sort((left, right) => left.startBeat - right.startBeat), [autoMixSections, totalBeats]);    const selectedNotesByPitch = useMemo(() => {        const notesByPitch = new Map();        (allTrackMelodyMode            ? allTrackNotes            : selectedTrackNotes.map((note) => ({ ...note, trackId: selectedTrack?.id ?? '' }))).forEach((note) => {            const pitchNotes = notesByPitch.get(note.pitch) ?? [];            pitchNotes.push(note);            notesByPitch.set(note.pitch, pitchNotes);        });        return notesByPitch;    }, [allTrackMelodyMode, allTrackNotes, selectedTrack?.id, selectedTrackNotes]);    const otherNotesByPitch = useMemo(() => {        if (allTrackMelodyMode)            return new Map();        const trackIds = [];        const noteArrays = [];        project.tracks.forEach((track) => {            if (track.id === selectedTrack?.id)                return;            trackIds.push(track.id);            noteArrays.push(project.notesByTrack[track.id] ?? []);        });        const cached = otherNotesByPitchCacheRef.current;        const cacheMatches = cached?.selectedTrackId === selectedTrack?.id &&            cached.trackIds.length === trackIds.length &&            cached.trackIds.every((trackId, index) => (trackId === trackIds[index] && cached.noteArrays[index] === noteArrays[index]));        if (cacheMatches)            return cached.value;        const notesByPitch = new Map();        trackIds.forEach((trackId, trackIndex) => {            noteArrays[trackIndex].forEach((note) => {                const pitchNotes = notesByPitch.get(note.pitch) ?? [];                pitchNotes.push({ ...note, trackId });                notesByPitch.set(note.pitch, pitchNotes);            });        });        otherNotesByPitchCacheRef.current = {            noteArrays,            selectedTrackId: selectedTrack?.id,            trackIds,            value: notesByPitch,        };        return notesByPitch;    }, [allTrackMelodyMode, project.notesByTrack, project.tracks, selectedTrack?.id]);    const instrumentDialogTrack = project.tracks.find((track) => track.id === instrumentMenuTrackId) ?? null;    const selectedInstrumentId = pendingInstrumentId ?? instrumentDialogTrack?.instrumentId ?? selectedTrack?.instrumentId ?? 'gm-0';    const { addTrack, closeInstrumentDialog, confirmInstrumentDialog, cycleTrackColor, deleteTrack, openInstrumentDialog, previewInstrumentChoice, selectTrack, updateTrack, } = useTrackActions({        instrumentDialogTrack,        pendingInstrumentId,        setInstrumentCategory,        setInstrumentMenuTrackId,        setPendingInstrumentId,        setProject,    });    const { addAutoMixSection, addAutoMixSectionAtBeat, applyAutoMixGenrePreset, deleteAutoMixSection, focusAutoMixSection, getAutoMixFocusTrackId, runAutoMixTracks, setAutoMixFocusTrack, updateAutoMixSection, } = useAutoMixActions({        getCurrentPlaybackBeat,        isAutoMixing,        isPlaying,        playbackBeat,        project,        selectedAutoMixSectionId,        setAutoMixGenrePreset,        setAutoMixPanelOpen,        setAutoMixReport,        setIsAutoMixing,        setProject,        setSelectedAutoMixSectionId,        startPlaybackAt,    });    const { addTrackPlacement, beginTrackPlacementDrag, deleteTrackPlacement, } = useArrangementActions({        beginHistoryBatch,        clampBeatToSong,        endHistoryBatch,        getCurrentPlaybackBeat,        projectRef,        selectTrack,        setActiveEditorTab,        setProject,        setSelectedTrackPlacementId,        snapBeatToGrid,        totalBeats,        trackPlacementDragRef,    });    const { finishKeyboardNote, startKeyboardNote, } = useKeyboardRecording({        activePlaybackTracksRef,        getMinimumPlaybackDrumSeconds,        isPlaying,        keyboardInputEnabled,        keyboardRecordingRef,        playbackBeatRef,        playbackStartMsRef,        playbackStartSecondsRef,        playbackTempoTimelineRef,        projectRef,        selectedTrack,        setProject,        setSelectedNoteIds,        totalBeats,        totalBeatsRef,    });    const { beginKeyPreview, changeHeldPreviewPitch, continueKeyPreview, startHeldPreview, stopHeldPreview, } = usePreviewNote({        heldPreviewRef,        keyPreviewRef,        previewTokenRef,        selectedInstrumentId: selectedTrack?.instrumentId,        setPressedPitch,    });    const { beginLassoSelection, finishLassoSelection, finishPatternSelection, selectAllNotes, selectNotesInPatternArea, updateLassoSelectionFromPointer, updatePatternSelectionFromPointer, updateSelectionBox, updateSelectionBoxFromNotes, } = usePianoRollSelection({        allTrackMelodyMode,        allTrackNotes,        cacheRollPointerGeometry,        closePianoRollContextMenu,        closeTrackContextMenu,        getCellFromPointer,        lassoSelectionRef,        patternSelectionRef,        pianoRollRef,        rollPitches,        rollPointerGeometryRef,        selectedTrackId: selectedTrack?.id,        selectedTrackNotes,        setEditMenuOpen,        setLassoPoints,        setProject,        setSelectedNoteIds,        setSelectionBox,        stepWidth,        stepsPerBeat,        totalBeats,        totalSteps,    });    const { captureRollPointer, eraseDraggedCellFromPointer, eraseRightDraggedCellFromPointer, getGrabStepOffset, moveDraggedNoteFromPointer, releaseRollPointerCapture, } = usePianoRollDrag({        changeHeldPreviewPitch,        eraseNoteAtCellOnce,        eraseRef,        getCellFromPointer,        noteDragRef,        pianoRollRef,        rightEraseRef,        rollPitches,        rollPointerCaptureRef,        setProject,        stepsPerBeat,        totalBeats,        totalSteps,        updateSelectionBoxFromNotes,    });    const { createNewProject, handleDragOver, handleDrop, importAudioFiles, loadProjectFile, openAudioUpload, openProjectFile, saveMidiFile, saveMp3File, saveProjectFile, toggleVoiceRecording, } = useFileActions({        audioFileInputRef,        fileInputRef,        getCurrentPlaybackBeat,        isExportingMp3,        isRecordingVoice,        mediaRecorderRef,        patternClipboardRef,        project,        projectRef,        recordingChunksRef,        recordingStartBeatRef,        recordingStartMsRef,        resetPlayback,        resetProjectHistory,        restoreProject,        selectedTrack,        setActiveEditorTab,        setFileMenuOpen,        setIsDraggingFile,        setIsExportingMp3,        setIsRecordingVoice,        setProject,        setSelectedNoteIds,        snapBeatToGrid,        totalBeats,    });    const instrumentCategories = useMemo(() => INSTRUMENT_CATEGORY_ORDER.filter((category) => INSTRUMENT_OPTIONS.some((instrument) => instrument.family === category)), []);    const categoryInstruments = useMemo(() => INSTRUMENT_OPTIONS.filter((instrument) => instrument.family === instrumentCategory), [instrumentCategory]);    useEffect(() => {        function closeMenusFromOutsidePointer(event) {            if (event.button === 2)                return;            const target = event.target;            if (target instanceof Element && target.closest('.track-context-menu'))                return;            setTrackContextMenu(null);            setPianoRollContextMenu(null);        }        window.addEventListener('pointerdown', closeMenusFromOutsidePointer, { capture: true });        return () => window.removeEventListener('pointerdown', closeMenusFromOutsidePointer, { capture: true });    }, []);    useEffect(() => {        const roll = pianoRollRef.current;        if (!roll)            return undefined;        function handleNativeWheel(event) {            if (!event.ctrlKey)                return;            event.preventDefault();            interactionHandlersRef.current.zoomRoll(event.deltaY > 0 ? -1 : 1);        }        roll.addEventListener('wheel', handleNativeWheel, { passive: false });        return () => roll.removeEventListener('wheel', handleNativeWheel);    }, []);    useEffect(() => {        playbackBeatRef.current = playbackBeat;        totalBeatsRef.current = totalBeats;        pianoRollRef.current?.style.setProperty('--playhead-left', `${Math.min(100, (playbackBeat / totalBeats) * 100)}%`);    }, [playbackBeat, totalBeats]);    useEffect(() => {        activeEditorTabRef.current = activeEditorTab;    }, [activeEditorTab]);    useEffect(() => {        autoMixPanelOpenRef.current = autoMixPanelOpen;    }, [autoMixPanelOpen]);    useEffect(() => {        setTempoInput(String(selectedTempoSection?.tempo ?? project.tempo));    }, [project.tempo, selectedTempoSection?.tempo]);    useEffect(() => {        if (autoMixSections.length === 0) {            if (selectedAutoMixSectionId !== null)                setSelectedAutoMixSectionId(null);            return;        }        if (!selectedAutoMixSectionId || !autoMixSections.some((section) => section.id === selectedAutoMixSectionId)) {            setSelectedAutoMixSectionId(autoMixSections[0]?.id ?? null);        }    }, [autoMixSections, selectedAutoMixSectionId]);    useEffect(() => {        if (tempoSections.length === 0) {            if (selectedTempoSectionId !== null)                setSelectedTempoSectionId(null);            return;        }        if (!selectedTempoSectionId || !tempoSections.some((section) => section.id === selectedTempoSectionId)) {            setSelectedTempoSectionId(tempoSections[0]?.id ?? null);        }    }, [selectedTempoSectionId, tempoSections]);    useEffect(() => {        const placements = project.patternPlacements ?? [];        if (placements.length === 0) {            if (selectedTrackPlacementId !== null)                setSelectedTrackPlacementId(null);            return;        }        if (!selectedTrackPlacementId || !placements.some((placement) => placement.id === selectedTrackPlacementId)) {            setSelectedTrackPlacementId(placements[0]?.id ?? null);        }    }, [project.patternPlacements, selectedTrackPlacementId]);    useEffect(() => {        setSelectedNoteIds([]);        setSelectionBox(null);        patternSelectionRef.current = null;        lassoSelectionRef.current = { active: false, points: [] };        setLassoPoints([]);    }, [selectedTrack?.id]);    useEffect(() => {        if (activeEditorTab !== 'piano-roll')            return;        if (selectedNoteIds.length === 0) {            if (!patternSelectionRef.current?.active && !patternRepeatRef.current?.active) {                setSelectionBox(null);            }            return;        }        if (patternSelectionRef.current?.active ||            patternRepeatRef.current?.active ||            noteDragRef.current?.active ||            lassoSelectionRef.current.active)            return;        const selectedNoteIdSetForBox = new Set(selectedNoteIds);        const selectedNotes = (allTrackMelodyMode ? allTrackNotes : selectedTrackNotes.map((note) => ({ ...note, trackId: selectedTrack?.id ?? '' })))            .filter((note) => selectedNoteIdSetForBox.has(note.id));        if (selectedNotes.length === 0) {            setSelectionBox(null);            return;        }        const rows = selectedNotes            .map((note) => rollPitches.indexOf(note.pitch))            .filter((rowIndex) => rowIndex >= 0);        if (rows.length === 0) {            setSelectionBox(null);            return;        }        const roll = pianoRollRef.current;        const gridWidth = Math.max(totalSteps * stepWidth, (roll?.clientWidth ?? 0) - KEY_COLUMN_WIDTH);        const cellWidth = gridWidth / totalSteps;        const startStep = Math.min(...selectedNotes.map((note) => Math.round(note.startBeat * stepsPerBeat)));        const endStep = Math.max(...selectedNotes.map((note) => Math.ceil((note.startBeat + note.durationBeats) * stepsPerBeat) - 1));        const startRow = Math.min(...rows);        const endRow = Math.max(...rows);        setSelectionBox({            height: (endRow - startRow + 1) * ROLL_ROW_HEIGHT,            left: KEY_COLUMN_WIDTH + startStep * cellWidth,            selecting: false,            top: ROLL_HEADER_HEIGHT + startRow * ROLL_ROW_HEIGHT,            width: (endStep - startStep + 1) * cellWidth,        });    }, [activeEditorTab, allTrackMelodyMode, allTrackNotes, rollPitches, selectedNoteIds, selectedTrack?.id, selectedTrackNotes, stepWidth, stepsPerBeat, totalSteps]);    interactionHandlersRef.current.addAutoMixSection = addAutoMixSection;    interactionHandlersRef.current.togglePlayback = togglePlayback;    interactionHandlersRef.current.copySelectedNotes = copySelectedNotes;    interactionHandlersRef.current.cutSelectedNotes = cutSelectedNotes;    interactionHandlersRef.current.deleteSelectedNote = deleteSelectedNote;    interactionHandlersRef.current.moveDraggedNoteFromPointer = moveDraggedNoteFromPointer;    interactionHandlersRef.current.eraseDraggedCellFromPointer = eraseDraggedCellFromPointer;    interactionHandlersRef.current.eraseRightDraggedCellFromPointer = eraseRightDraggedCellFromPointer;    interactionHandlersRef.current.finishLassoSelection = finishLassoSelection;    interactionHandlersRef.current.finishPatternSelection = finishPatternSelection;    interactionHandlersRef.current.finishPatternRepeat = finishPatternRepeat;    interactionHandlersRef.current.finishKeyboardNote = finishKeyboardNote;    interactionHandlersRef.current.startKeyboardNote = startKeyboardNote;    interactionHandlersRef.current.transposeSelectedNotes = transposeSelectedNotes;    interactionHandlersRef.current.ungroupSelectedPattern = ungroupSelectedPattern;    interactionHandlersRef.current.updatePatternRepeatPreview = updatePatternRepeatPreview;    interactionHandlersRef.current.updateLassoSelectionFromPointer = updateLassoSelectionFromPointer;    interactionHandlersRef.current.zoomRoll = zoomRoll;    interactionHandlersRef.current.pasteSelectedNotes = pasteSelectedNotes;    interactionHandlersRef.current.redoProject = redoProject;    interactionHandlersRef.current.selectAllNotes = selectAllNotes;    interactionHandlersRef.current.undoProject = undoProject;    interactionHandlersRef.current.updatePatternSelectionFromPointer = updatePatternSelectionFromPointer;    interactionHandlersRef.current.stopHeldPreview = stopHeldPreview;    useEffect(() => {        function handleKeyDown(event) {            if (event.ctrlKey || event.metaKey) {                if (event.code === 'KeyZ') {                    event.preventDefault();                    if (event.shiftKey) {                        interactionHandlersRef.current.redoProject();                    }                    else {                        interactionHandlersRef.current.undoProject();                    }                    return;                }                if (event.code === 'KeyY') {                    event.preventDefault();                    interactionHandlersRef.current.redoProject();                    return;                }                if (event.code === 'KeyU') {                    event.preventDefault();                    interactionHandlersRef.current.ungroupSelectedPattern();                    return;                }                if (event.code === 'KeyC') {                    event.preventDefault();                    interactionHandlersRef.current.copySelectedNotes();                    return;                }                if (event.code === 'KeyA') {                    event.preventDefault();                    interactionHandlersRef.current.selectAllNotes();                    return;                }                if (event.code === 'KeyX') {                    event.preventDefault();                    interactionHandlersRef.current.cutSelectedNotes();                    return;                }                if (event.code === 'KeyV') {                    event.preventDefault();                    interactionHandlersRef.current.pasteSelectedNotes();                    return;                }                if (event.code === 'Equal' || event.code === 'NumpadAdd') {                    event.preventDefault();                    interactionHandlersRef.current.zoomRoll(1);                    return;                }                if (event.code === 'Minus' || event.code === 'NumpadSubtract') {                    event.preventDefault();                    interactionHandlersRef.current.zoomRoll(-1);                    return;                }            }            const target = event.target;            const isTypingTarget = target instanceof HTMLInputElement ||                target instanceof HTMLTextAreaElement ||                target instanceof HTMLSelectElement ||                (target instanceof HTMLElement && target.isContentEditable);            if (event.code === 'Escape') {                closeTrackContextMenu();                closePianoRollContextMenu();                return;            }            if (!isTypingTarget &&                !event.repeat &&                event.code === 'KeyC' &&                (activeEditorTabRef.current === 'automix' || autoMixPanelOpenRef.current)) {                event.preventDefault();                interactionHandlersRef.current.addAutoMixSection();                return;            }            if (!event.repeat && !isTypingTarget) {                interactionHandlersRef.current.startKeyboardNote(event.code, event.timeStamp);            }            if (event.code === 'Delete' || event.code === 'Backspace') {                if (isTypingTarget) {                    return;                }                event.preventDefault();                interactionHandlersRef.current.deleteSelectedNote();                return;            }            if (event.code === 'ArrowUp' || event.code === 'ArrowDown') {                if (isTypingTarget) {                    return;                }                event.preventDefault();                interactionHandlersRef.current.transposeSelectedNotes(event.code === 'ArrowUp' ? 1 : -1);                return;            }            if (event.code !== 'Space')                return;            event.preventDefault();            interactionHandlersRef.current.togglePlayback();        }        function handleKeyUp(event) {            interactionHandlersRef.current.finishKeyboardNote(event.code, event.timeStamp);        }        function flushPointerMove() {            pointerMoveFrameRef.current = 0;            const pendingMove = pendingPointerMoveRef.current;            pendingPointerMoveRef.current = null;            if (!pendingMove)                return;            const rightClickAction = rightClickRollActionRef.current;            if (rightClickAction?.active && (pendingMove.buttons & 2) === 2) {                const movedDistance = Math.hypot(pendingMove.clientX - rightClickAction.startX, pendingMove.clientY - rightClickAction.startY);                if (movedDistance > 4) {                    rightClickAction.moved = true;                }            }            interactionHandlersRef.current.moveDraggedNoteFromPointer(pendingMove.clientX, pendingMove.clientY);            interactionHandlersRef.current.updatePatternRepeatPreview(pendingMove.clientX);            interactionHandlersRef.current.eraseDraggedCellFromPointer(pendingMove.clientX, pendingMove.clientY);            interactionHandlersRef.current.updatePatternSelectionFromPointer(pendingMove.clientX, pendingMove.clientY);            interactionHandlersRef.current.updateLassoSelectionFromPointer(pendingMove.clientX, pendingMove.clientY);            if (rightEraseRef.current.active && (pendingMove.buttons & 2) !== 2) {                rightEraseRef.current = { active: false, lastKey: '' };                erasedNoteIdsRef.current = new Set();                return;            }            interactionHandlersRef.current.eraseRightDraggedCellFromPointer(pendingMove.clientX, pendingMove.clientY);        }        function handlePointerMove(event) {            const hasActivePointerTask = Boolean(noteDragRef.current?.active) ||                Boolean(patternSelectionRef.current?.active) ||                Boolean(patternRepeatRef.current?.active) ||                lassoSelectionRef.current.active ||                eraseRef.current.active ||                rightEraseRef.current.active;            if (!hasActivePointerTask)                return;            event.preventDefault();            pendingPointerMoveRef.current = {                buttons: event.buttons,                clientX: event.clientX,                clientY: event.clientY,            };            if (pointerMoveFrameRef.current === 0) {                pointerMoveFrameRef.current = window.requestAnimationFrame(flushPointerMove);            }        }        function clearPointerState(event) {            if (pointerMoveFrameRef.current !== 0) {                window.cancelAnimationFrame(pointerMoveFrameRef.current);                flushPointerMove();            }            const rightClickAction = rightClickRollActionRef.current;            pendingPointerMoveRef.current = null;            interactionHandlersRef.current.finishLassoSelection();            interactionHandlersRef.current.finishPatternSelection();            interactionHandlersRef.current.finishPatternRepeat();            noteDragRef.current = null;            patternSelectionRef.current = null;            lassoSelectionRef.current = { active: false, points: [] };            setLassoPoints([]);            eraseRef.current = { active: false, lastKey: '' };            rightEraseRef.current = { active: false, lastKey: '' };            rightClickRollActionRef.current = null;            erasedNoteIdsRef.current = new Set();            keyPreviewRef.current.active = false;            rollPointerGeometryRef.current = null;            setDraggingNoteId(null);            releaseRollPointerCapture(event?.pointerId);            interactionHandlersRef.current.stopHeldPreview();            endHistoryBatch();            if (event?.type === 'pointerup' &&                event.button === 2 &&                rightClickAction?.active &&                !rightClickAction.moved &&                !rightClickAction.deleted) {                setPianoRollContextMenu({                    x: rightClickAction.menuX,                    y: rightClickAction.menuY,                });            }        }        function suppressNativeContextMenu(event) {            if (rightClickRollActionRef.current || pianoRollRef.current?.contains(event.target)) {                event.preventDefault();            }        }        function clearPointerStateOnBlur() {            clearPointerState();        }        window.addEventListener('keydown', handleKeyDown);        window.addEventListener('keyup', handleKeyUp);        window.addEventListener('pointermove', handlePointerMove, { passive: false });        window.addEventListener('pointerup', clearPointerState);        window.addEventListener('pointercancel', clearPointerState);        window.addEventListener('contextmenu', suppressNativeContextMenu);        window.addEventListener('blur', clearPointerStateOnBlur);        return () => {            window.removeEventListener('keydown', handleKeyDown);            window.removeEventListener('keyup', handleKeyUp);            window.removeEventListener('pointermove', handlePointerMove);            window.removeEventListener('pointerup', clearPointerState);            window.removeEventListener('pointercancel', clearPointerState);            window.removeEventListener('contextmenu', suppressNativeContextMenu);            window.removeEventListener('blur', clearPointerStateOnBlur);            if (pointerMoveFrameRef.current !== 0) {                window.cancelAnimationFrame(pointerMoveFrameRef.current);            }        };    }, []);    useEffect(() => {        if (!isPlaying)            return;        let frameId = 0;        const tick = () => {            const now = performance.now();            const playbackTotalBeats = Math.max(1, totalBeatsRef.current);            const timeline = playbackTempoTimelineRef.current.length > 0                ? playbackTempoTimelineRef.current                : buildTempoTimeline(projectRef.current, playbackTotalBeats);            const currentBeat = Math.min(playbackTotalBeats, getBeatAtSecondsFromTimeline(timeline, playbackStartSecondsRef.current + (now - playbackStartMsRef.current) / 1000, playbackTotalBeats));            playbackBeatRef.current = currentBeat;            pianoRollRef.current?.style.setProperty('--playhead-left', `${Math.min(100, (currentBeat / totalBeatsRef.current) * 100)}%`);            const roll = pianoRollRef.current;            const userIsEditingRoll = Boolean(noteDragRef.current?.active) ||                Boolean(patternSelectionRef.current?.active) ||                Boolean(patternRepeatRef.current?.active) ||                lassoSelectionRef.current.active ||                eraseRef.current.active ||                rightEraseRef.current.active;            if (roll &&                totalBeatsRef.current > 0 &&                !userIsEditingRoll &&                now - lastPlayheadAutoScrollAtRef.current >= PLAYHEAD_AUTO_SCROLL_THROTTLE_MS) {                lastPlayheadAutoScrollAtRef.current = now;                const gridWidth = Math.max(totalBeatsRef.current * DEFAULT_BEAT_WIDTH * rollZoom, roll.scrollWidth - KEY_COLUMN_WIDTH);                const playheadX = KEY_COLUMN_WIDTH + (currentBeat / totalBeatsRef.current) * gridWidth;                const viewportLeft = roll.scrollLeft;                const viewportRight = viewportLeft + roll.clientWidth;                if (playheadX > viewportRight - PLAYHEAD_SCROLL_PADDING) {                    roll.scrollLeft = Math.min(roll.scrollWidth - roll.clientWidth, playheadX - roll.clientWidth + PLAYHEAD_SCROLL_PADDING);                }                else if (playheadX < viewportLeft + KEY_COLUMN_WIDTH + PLAYHEAD_SCROLL_PADDING) {                    roll.scrollLeft = Math.max(0, playheadX - KEY_COLUMN_WIDTH - PLAYHEAD_SCROLL_PADDING);                }            }            frameId = window.requestAnimationFrame(tick);        };        frameId = window.requestAnimationFrame(tick);        return () => window.cancelAnimationFrame(frameId);    }, [isPlaying, rollZoom]);    function updateProjectTitle(title) {        setProject((current) => (current.title === title ? current : { ...current, title }));    }    function openTrackContextMenu(trackId, event) {        event.preventDefault();        event.stopPropagation();        selectTrack(trackId);        setTrackContextMenu({            trackId,            x: event.clientX,            y: event.clientY,        });    }    function openTrackPanelContextMenu(event) {        if (event.currentTarget !== event.target)            return;        event.preventDefault();        event.stopPropagation();        setPianoRollContextMenu(null);        setTrackContextMenu({            trackId: selectedTrack?.id ?? project.tracks[0].id,            x: event.clientX,            y: event.clientY,        });    }    function beginRightClickRollAction(event, options = {}) {        if (event.button !== 2)            return;        event.preventDefault();        event.stopPropagation();        setTrackContextMenu(null);        setPianoRollContextMenu(null);        cacheRollPointerGeometry();        if (options.deleteImmediately) {            beginHistoryBatch();        }        rightEraseRef.current = { active: true, lastKey: '' };        erasedNoteIdsRef.current = new Set(options.note ? [options.note.id] : []);        rightClickRollActionRef.current = {            active: true,            deleted: Boolean(options.deleteImmediately),            menuX: event.clientX,            menuY: event.clientY,            moved: false,            startX: event.clientX,            startY: event.clientY,        };        if (options.deleteImmediately && options.note) {            deleteNote(options.note.id);        }    }    function cacheRollPointerGeometry() {        const roll = pianoRollRef.current;        if (!roll) {            rollPointerGeometryRef.current = null;            return;        }        rollPointerGeometryRef.current = {            gridWidth: Math.max(totalSteps * stepWidth, roll.clientWidth - KEY_COLUMN_WIDTH),            rect: roll.getBoundingClientRect(),            totalSteps,        };    }    function getCellFromPointer(clientX, clientY) {        const roll = pianoRollRef.current;        if (!roll)            return null;        const geometry = rollPointerGeometryRef.current;        const rect = geometry?.rect ?? roll.getBoundingClientRect();        const pointerTotalSteps = geometry?.totalSteps ?? totalSteps;        const gridWidth = geometry?.gridWidth ?? Math.max(pointerTotalSteps * stepWidth, roll.clientWidth - KEY_COLUMN_WIDTH);        const x = clientX - rect.left - KEY_COLUMN_WIDTH + roll.scrollLeft;        const y = clientY - rect.top - ROLL_HEADER_HEIGHT + roll.scrollTop;        const step = Math.floor((x / gridWidth) * pointerTotalSteps);        const rowIndex = Math.floor(y / ROLL_ROW_HEIGHT);        if (step < 0 || step >= pointerTotalSteps || rowIndex < 0 || rowIndex >= rollPitches.length)            return null;        return { pitch: rollPitches[rowIndex], rowIndex, step };    }    function getStepFromRowPointer(row, clientX) {        const rect = row.getBoundingClientRect();        const x = Math.min(Math.max(clientX - rect.left, 0), rect.width);        return Math.min(totalSteps - 1, Math.max(0, Math.floor((x / rect.width) * totalSteps)));    }    function snapBeatToGrid(beat) {        return Math.max(0, Math.round(beat * stepsPerBeat) / stepsPerBeat);    }    function clampBeatToSong(beat, durationBeats = 0) {        return Math.max(0, Math.min(totalBeats - durationBeats, beat));    }    function getRowLabel(pitch) {        if (selectedTrack && isDrumInstrument(selectedTrack.instrumentId)) {            return DRUM_LABELS[pitch] ?? `Drum ${pitch}`;        }        const name = getPitchName(pitch);        return name.startsWith('C') ? name : '';    }    function getNoteDisplayLabel(note) {        if (selectedTrack && isDrumInstrument(selectedTrack.instrumentId)) {            return DRUM_LABELS[note.pitch] ?? `Drum ${note.pitch}`;        }        return getPitchName(note.pitch);    }    function getNoteTrackId(note, fallbackTrackId = selectedTrack?.id ?? '') {        return 'trackId' in note && typeof note.trackId === 'string' ? note.trackId : fallbackTrackId;    }    function findEditableNoteAtCell(pitch, step) {        const beat = step / stepsPerBeat;        const notePool = allTrackMelodyMode            ? allTrackNotes            : selectedTrackNotes.map((note) => ({ ...note, trackId: selectedTrack?.id ?? '' }));        return notePool.find((note) => note.pitch === pitch &&            beat >= note.startBeat &&            beat < note.startBeat + note.durationBeats) ?? null;    }    function beginSelectionBoxMove(event) {        if (event.button !== 0 || selectedPatternNotes.length === 0)            return;        const pointerCell = getCellFromPointer(event.clientX, event.clientY);        if (!pointerCell)            return;        event.preventDefault();        event.stopPropagation();        captureRollPointer(event.pointerId, event.currentTarget);        cacheRollPointerGeometry();        const selectedRows = selectedPatternNotes            .map((note) => rollPitches.indexOf(note.pitch))            .filter((rowIndex) => rowIndex >= 0);        if (selectedRows.length === 0)            return;        const topRow = Math.min(...selectedRows);        const originPitch = rollPitches[topRow];        const originStep = Math.min(...selectedPatternNotes.map((note) => Math.round(note.startBeat * stepsPerBeat)));        const firstNote = selectedPatternNotes[0];        beginHistoryBatch();        noteDragRef.current = {            active: true,            grabPitchOffset: pointerCell.pitch - originPitch,            grabStepOffset: Math.max(0, pointerCell.step - originStep),            groupNoteIds: selectedPatternNotes.map((note) => note.id),            noteId: firstNote.id,            originalNotes: selectedPatternNotes,            originPitch,            originStep,            trackId: firstNote.trackId,            lastPitch: pointerCell.pitch,            lastStep: pointerCell.step,        };        setDraggingNoteId(firstNote.id);    }    function eraseNoteAtCell(pitch, step) {        const noteToDelete = findEditableNoteAtCell(pitch, step);        if (!noteToDelete || erasedNoteIdsRef.current.has(noteToDelete.id))            return false;        const trackId = noteToDelete.trackId;        erasedNoteIdsRef.current.add(noteToDelete.id);        if (rightClickRollActionRef.current?.active) {            rightClickRollActionRef.current.deleted = true;        }        beginHistoryBatch();        setProject((current) => {            const nextProject = {                ...current,                selectedTrackId: trackId,                selectedNoteId: null,                notesByTrack: {                    ...current.notesByTrack,                    [trackId]: (current.notesByTrack[trackId] ?? []).filter((note) => note.id !== noteToDelete.id),                },            };            projectRef.current = nextProject;            return nextProject;        });        return true;    }    function eraseNoteAtCellOnce(pitch, step, eraseState) {        const key = `${pitch}-${step}`;        if (eraseState.lastKey === key)            return false;        eraseState.lastKey = key;        return eraseNoteAtCell(pitch, step);    }    function beginPatternSelection(pitch, step, event) {        const rowIndex = rollPitches.indexOf(pitch);        if (rowIndex < 0)            return;        event.preventDefault();        event.stopPropagation();        setSelectionBox(null);        cacheRollPointerGeometry();        const selection = {            active: true,            endRow: rowIndex,            endStep: step,            startRow: rowIndex,            startStep: step,        };        patternSelectionRef.current = selection;        setSelectedNoteIds([]);        updateSelectionBox(selection);        selectNotesInPatternArea(selection);    }    function changeToolMode(nextToolMode) {        setToolMode(nextToolMode);        if (nextToolMode !== 'select') {            setSelectionBox(null);            patternSelectionRef.current = null;        }        if (nextToolMode !== 'lasso') {            lassoSelectionRef.current = { active: false, points: [] };            setLassoPoints([]);        }    }    function beginMoveNote(note, event) {        if (!selectedTrack || event.button !== 0)            return;        event.preventDefault();        event.stopPropagation();        const noteTrackId = getNoteTrackId(note, selectedTrack.id);        const noteGroup = patternRepeatGroupByNoteId.get(note.id) ?? null;        const groupedNoteIds = noteGroup            ? getPatternRepeatGroupNoteIds(noteGroup).filter((noteId) => allTrackNoteIdSet.has(noteId))            : null;        const isSelectedNote = selectedNoteIdSet.has(note.id);        const isPatternMember = isSelectedNote && selectedNoteIds.length > 1;        if (toolMode === 'select' && !isSelectedNote) {            const nextSelectedIds = groupedNoteIds ?? [note.id];            setSelectedNoteIds(nextSelectedIds);            if (nextSelectedIds.length > 1) {                const selectedIdSet = new Set(nextSelectedIds);                updateSelectionBoxFromNotes(allTrackNotes.filter((item) => selectedIdSet.has(item.id)));            }            else {                setSelectionBox(null);            }            lastNoteDurationRef.current = note.durationBeats;            setProject((current) => current.selectedNoteId === nextSelectedIds[0] && current.selectedTrackId === noteTrackId                ? current                : { ...current, selectedTrackId: noteTrackId, selectedNoteId: nextSelectedIds[0] });            return;        }        if (toolMode === 'erase') {            beginHistoryBatch();            cacheRollPointerGeometry();            eraseRef.current = { active: true, lastKey: '' };            erasedNoteIdsRef.current = new Set([note.id]);            deleteNote(note.id);            return;        }        setProject((current) => current.selectedNoteId === note.id && current.selectedTrackId === noteTrackId            ? current            : { ...current, selectedTrackId: noteTrackId, selectedNoteId: note.id });        const effectiveGroupNoteIds = isPatternMember ? selectedNoteIds : (groupedNoteIds ?? [note.id]);        if (!isPatternMember && effectiveGroupNoteIds.length > 0) {            setSelectedNoteIds(effectiveGroupNoteIds);            if (effectiveGroupNoteIds.length === 1) {                setSelectionBox(null);            }            else {                const selectedIdSet = new Set(effectiveGroupNoteIds);                updateSelectionBoxFromNotes(allTrackNotes.filter((item) => selectedIdSet.has(item.id)));            }        }        lastNoteDurationRef.current = note.durationBeats;        setDraggingNoteId(note.id);        cacheRollPointerGeometry();        const pointerCell = getCellFromPointer(event.clientX, event.clientY);        const noteStartStep = Math.round(note.startBeat * stepsPerBeat);        const groupNoteIds = effectiveGroupNoteIds.length > 0 ? effectiveGroupNoteIds : [note.id];        const groupNoteIdSet = new Set(groupNoteIds);        const originalNotes = groupNoteIds.length > 1            ? allTrackNotes.filter((item) => groupNoteIdSet.has(item.id))            : [{ ...note, trackId: noteTrackId }];        beginHistoryBatch();        noteDragRef.current = {            active: true,            grabPitchOffset: 0,            grabStepOffset: getGrabStepOffset(note, pointerCell?.step ?? null),            groupNoteIds,            noteId: note.id,            originalNotes,            originPitch: note.pitch,            originStep: noteStartStep,            trackId: noteTrackId,            lastPitch: note.pitch,            lastStep: pointerCell?.step ?? noteStartStep,        };        startHeldPreview(note.pitch, note.velocity, projectRef.current.tracks.find((track) => track.id === noteTrackId)?.instrumentId);    }    function beginPatternRepeat(event) {        if (event.button !== 0 || selectedPatternNotes.length === 0 || !selectionBox)            return;        event.preventDefault();        event.stopPropagation();        const selectedGroup = selectedPatternRepeatGroup;        const selectedGroupNoteIds = selectedGroup ? new Set(selectedGroup.baseNoteIds) : null;        const baseNotes = selectedGroupNoteIds            ? selectedPatternNotes.filter((note) => selectedGroupNoteIds.has(note.id))            : selectedPatternNotes;        const baseStartBeat = Math.min(...baseNotes.map((note) => note.startBeat));        const baseEndBeat = Math.max(...baseNotes.map((note) => note.startBeat + note.durationBeats));        if (baseEndBeat <= baseStartBeat)            return;        const baseRepeatCount = selectedGroup ? selectedGroup.repeats.length + 1 : 1;        const gapBeats = selectedGroup?.gapBeats ?? getCurrentPatternRepeatGapBeats();        const gapWidth = gapBeats * beatWidth;        const baseWidth = selectedGroup            ? (selectionBox.width - gapWidth * (baseRepeatCount - 1)) / baseRepeatCount            : selectionBox.width;        beginHistoryBatch();        patternRepeatRef.current = {            active: true,            baseEndBeat,            baseStartBeat,            baseWidth,            currentWidth: selectionBox.width,            existingGroupId: selectedGroup?.id ?? null,            gapBeats,            notes: baseNotes,            repeatCount: baseRepeatCount,            startClientX: event.clientX,            startRepeatCount: baseRepeatCount,        };    }    function updatePatternRepeatPreview(clientX) {        const repeat = patternRepeatRef.current;        if (!repeat?.active)            return;        const gapWidth = repeat.gapBeats * beatWidth;        const unitWidth = Math.max(1, repeat.baseWidth + gapWidth);        const dragDistance = clientX - repeat.startClientX;        const stepDelta = Math.trunc(dragDistance / unitWidth);        const nextRepeatCount = Math.max(1, repeat.startRepeatCount + stepDelta);        if (nextRepeatCount === repeat.repeatCount)            return;        const nextWidth = repeat.baseWidth * nextRepeatCount + gapWidth * (nextRepeatCount - 1);        repeat.repeatCount = nextRepeatCount;        repeat.currentWidth = nextWidth;        setSelectionBox((current) => (current ? { ...current, selecting: false, width: nextWidth } : current));    }    function finishPatternRepeat() {        const repeat = patternRepeatRef.current;        if (!repeat?.active)            return;        patternRepeatRef.current = null;        const repeatCount = repeat.repeatCount;        const currentGroup = repeat.existingGroupId            ? projectRef.current.patternRepeatGroups?.find((group) => group.id === repeat.existingGroupId) ?? null            : null;        const currentRepeatCount = currentGroup ? currentGroup.repeats.length + 1 : 1;        if (!currentGroup && repeatCount <= 1)            return;        if (currentGroup && repeatCount === currentRepeatCount)            return;        const spanBeats = repeat.baseEndBeat - repeat.baseStartBeat;        const repeatStepBeats = spanBeats + repeat.gapBeats;        const nextNotes = [];        const nextRepeatNoteIds = [];        for (let repeatIndex = currentRepeatCount; repeatIndex < repeatCount; repeatIndex += 1) {            const beatOffset = repeatStepBeats * repeatIndex;            const repeatNoteIds = [];            repeat.notes.forEach((note) => {                const nextNote = {                    ...note,                    id: createId('note'),                    startBeat: snapBeatToGrid(note.startBeat + beatOffset),                };                repeatNoteIds.push(nextNote.id);                nextNotes.push(nextNote);            });            nextRepeatNoteIds.push(repeatNoteIds);        }        const keptRepeats = currentGroup?.repeats.slice(0, Math.max(0, repeatCount - 1)) ?? [];        const removedNoteIds = new Set(currentGroup && repeatCount < currentRepeatCount            ? currentGroup.repeats.slice(Math.max(0, repeatCount - 1)).flatMap((item) => item.noteIds)            : []);        const nextRepeats = [            ...keptRepeats,            ...nextRepeatNoteIds.map((noteIds) => ({ noteIds })),        ];        const nextSelectedIds = [            ...repeat.notes.map((note) => note.id),            ...nextRepeats.flatMap((item) => item.noteIds),        ];        const nextSelectionNotes = [            ...repeat.notes,            ...allTrackNotes.filter((note) => nextSelectedIds.includes(note.id)),            ...nextNotes,        ].filter((note, index, notes) => notes.findIndex((item) => item.id === note.id) === index);        setSelectedNoteIds(nextSelectedIds);        updateSelectionBoxFromNotes(nextSelectionNotes);        setProject((current) => ({            ...current,            selectedNoteId: nextSelectedIds[0] ?? current.selectedNoteId,            notesByTrack: Object.fromEntries(Object.entries(current.notesByTrack).map(([trackId, notes]) => [                trackId,                [                    ...notes.filter((note) => !removedNoteIds.has(note.id)),                    ...nextNotes                        .filter((note) => note.trackId === trackId)                        .map(toStoredNote),                ],            ])),            patternRepeatGroups: [                ...(current.patternRepeatGroups ?? []).filter((group) => group.id !== repeat.existingGroupId),                ...(nextRepeats.length > 0                    ? [{                            id: currentGroup?.id ?? createId('pattern-group'),                            baseEndBeat: repeat.baseEndBeat,                            baseNoteIds: repeat.notes.map((note) => note.id),                            baseStartBeat: repeat.baseStartBeat,                            gapBeats: repeat.gapBeats,                            repeats: nextRepeats,                        }]                    : []),            ],        }));    }    function ungroupSelectedPattern() {        const group = selectedPatternRepeatGroup;        if (!group)            return;        setProject((current) => ({            ...current,            patternRepeatGroups: (current.patternRepeatGroups ?? []).filter((item) => item.id !== group.id),        }));    }    function getCurrentPatternRepeatGapSteps() {        const parsed = Math.round(Number(patternRepeatGapInput));        if (!Number.isFinite(parsed))            return 1;        return Math.max(0, Math.min(512, parsed));    }    function getCurrentPatternRepeatGapBeats() {        return normalizePatternRepeatGapBeats(getCurrentPatternRepeatGapSteps() / stepsPerBeat);    }    function commitPatternRepeatGap() {        const nextGapSteps = getCurrentPatternRepeatGapSteps();        const nextGapBeats = getCurrentPatternRepeatGapBeats();        setPatternRepeatGapInput(String(nextGapSteps));        const group = selectedPatternRepeatGroup;        if (!group)            return;        setProject((current) => {            const nextGroups = (current.patternRepeatGroups ?? []).map((item) => (item.id === group.id                ? { ...item, gapBeats: nextGapBeats }                : item));            return { ...current, patternRepeatGroups: nextGroups };        });    }    function beginRightEraseNote(note, event) {        if (event.button !== 2)            return;        beginRightClickRollAction(event, { deleteImmediately: true, note });    }    function beginCellAction(pitch, step, event) {        if (!selectedTrack)            return;        if (event.button === 2) {            beginRightClickRollAction(event);            return;        }        if (event.button !== 0)            return;        event.preventDefault();        if (toolMode === 'erase') {            cacheRollPointerGeometry();            beginHistoryBatch();            eraseRef.current = { active: true, lastKey: '' };            erasedNoteIdsRef.current = new Set();            eraseNoteAtCellOnce(pitch, step, eraseRef.current);            return;        }        if (toolMode === 'lasso' || event.altKey) {            beginLassoSelection(event);            return;        }        if (toolMode === 'select' || event.shiftKey) {            beginPatternSelection(pitch, step, event);            return;        }        const existingNote = findEditableNoteAtCell(pitch, step);        if (existingNote) {            const noteTrackId = existingNote.trackId;            const noteGroup = patternRepeatGroupByNoteId.get(existingNote.id) ?? null;            const groupedNoteIds = noteGroup                ? getPatternRepeatGroupNoteIds(noteGroup).filter((noteId) => allTrackNoteIdSet.has(noteId))                : [existingNote.id];            const groupedNoteIdSet = new Set(groupedNoteIds);            const originalNotes = groupedNoteIds.length > 1                ? allTrackNotes.filter((note) => groupedNoteIdSet.has(note.id))                : [existingNote];            setDraggingNoteId(existingNote.id);            cacheRollPointerGeometry();            const existingNoteStartStep = Math.round(existingNote.startBeat * stepsPerBeat);            beginHistoryBatch();            noteDragRef.current = {                active: true,                grabPitchOffset: 0,                grabStepOffset: getGrabStepOffset(existingNote, step),                groupNoteIds: groupedNoteIds,                noteId: existingNote.id,                originalNotes,                originPitch: pitch,                originStep: existingNoteStartStep,                trackId: noteTrackId,                lastPitch: pitch,                lastStep: step,            };            setProject((current) => current.selectedNoteId === existingNote.id && current.selectedTrackId === noteTrackId                ? current                : { ...current, selectedTrackId: noteTrackId, selectedNoteId: existingNote.id });            setSelectedNoteIds(groupedNoteIds);            if (groupedNoteIds.length > 1) {                updateSelectionBoxFromNotes(originalNotes);            }            else {                setSelectionBox(null);            }            lastNoteDurationRef.current = existingNote.durationBeats;            startHeldPreview(pitch, existingNote.velocity, projectRef.current.tracks.find((track) => track.id === noteTrackId)?.instrumentId);            return;        }        const note = {            id: createId('note'),            pitch,            startBeat: step / stepsPerBeat,            durationBeats: Math.min(defaultDurationBeats, totalBeats - step / stepsPerBeat),            velocity: 0.78,            pitchBend: 0,            volume: 1,            pan: 0,            expression: 1,            modulation: 0,        };        cacheRollPointerGeometry();        beginHistoryBatch();        noteDragRef.current = {            active: true,            grabPitchOffset: 0,            grabStepOffset: 0,            groupNoteIds: [note.id],            noteId: note.id,            originalNotes: [{ ...note, trackId: selectedTrack.id }],            originPitch: pitch,            originStep: step,            trackId: selectedTrack.id,            lastPitch: pitch,            lastStep: step,        };        setDraggingNoteId(note.id);        setSelectedNoteIds([note.id]);        lastNoteDurationRef.current = note.durationBeats;        startHeldPreview(pitch, note.velocity);        setProject((current) => ({            ...current,            selectedNoteId: note.id,            notesByTrack: {                ...current.notesByTrack,                [selectedTrack.id]: [...(current.notesByTrack[selectedTrack.id] ?? []), note],            },        }));    }    function beginRowAction(pitch, event) {        beginCellAction(pitch, getStepFromRowPointer(event.currentTarget, event.clientX), event);    }    function beginRowContextErase(_pitch, event) {        event.preventDefault();    }    function deleteSelectedNote() {        if (selectedNoteIds.length > 1) {            deleteSelectedNotes();            return;        }        if (!selectedNote)            return;        deleteNote(selectedNote.id);    }    function deleteSelectedNotes() {        if (selectedNoteIds.length === 0)            return;        const idsToDelete = new Set(selectedNoteIds);        setSelectedNoteIds([]);        setSelectionBox(null);        setProject((current) => ({            ...current,            selectedNoteId: null,            notesByTrack: Object.fromEntries(Object.entries(current.notesByTrack).map(([trackId, notes]) => [                trackId,                notes.filter((note) => !idsToDelete.has(note.id)),            ])),            patternRepeatGroups: prunePatternRepeatGroups(current.patternRepeatGroups, idsToDelete),        }));    }    function transposeSelectedNotes(direction) {        if (editableSelectedNotes.length === 0)            return;        const pitchStep = getGroupedPitchStep(editableSelectedNotes);        const pitchDelta = direction * pitchStep;        const targetIds = new Set(editableSelectedNotes.map((note) => note.id));        const targetNotes = allTrackNotes.filter((note) => targetIds.has(note.id));        if (targetNotes.length === 0)            return;        const minNextPitch = Math.min(...targetNotes.map((note) => note.pitch + pitchDelta));        const maxNextPitch = Math.max(...targetNotes.map((note) => note.pitch + pitchDelta));        if (minNextPitch < 0 || maxNextPitch > 127)            return;        setProject((current) => ({            ...current,            notesByTrack: Object.fromEntries(Object.entries(current.notesByTrack).map(([trackId, notes]) => [                trackId,                notes.map((note) => (targetIds.has(note.id)                    ? { ...note, pitch: note.pitch + pitchDelta }                    : note)),            ])),        }));    }    function deleteNote(noteId) {        setSelectedNoteIds((current) => current.filter((id) => id !== noteId));        setSelectionBox(null);        setProject((current) => ({            ...current,            selectedNoteId: current.selectedNoteId === noteId ? null : current.selectedNoteId,            notesByTrack: Object.fromEntries(Object.entries(current.notesByTrack).map(([trackId, notes]) => [                trackId,                notes.filter((note) => note.id !== noteId),            ])),            patternRepeatGroups: prunePatternRepeatGroups(current.patternRepeatGroups, new Set([noteId])),        }));    }    function getSelectedNoteValue(key) {        return getSelectedNoteValueFromNotes(editableSelectedNotes, key);    }    function updateSingleNoteControlValue(noteId, key, value) {        const clamped = clampNoteControlValue(key, value);        setProject((current) => {            let changed = false;            const nextNotesByTrack = Object.fromEntries(Object.entries(current.notesByTrack).map(([trackId, notes]) => [                trackId,                notes.map((note) => {                    if (note.id !== noteId)                        return note;                    const currentValue = getNoteControlValue(note, key);                    if (nearlyEqual(currentValue, clamped))                        return note;                    changed = true;                    return { ...note, [key]: clamped };                }),            ]));            return changed                ? { ...current, notesByTrack: nextNotesByTrack }                : current;        });    }    function updateDetailGraphSegmentValues(key, noteIds, beatA, valueA, beatB, valueB, min, max, step) {        const segmentStartBeat = Math.min(beatA, beatB);        const segmentEndBeat = Math.max(beatA, beatB);        const segmentSpan = Math.max(0.0001, segmentEndBeat - segmentStartBeat);        const updates = new Map();        noteIds.forEach((noteId) => {            const note = allTrackNotesById.get(noteId);            if (!note)                return;            if (note.startBeat < segmentStartBeat || note.startBeat > segmentEndBeat)                return;            const ratio = (note.startBeat - segmentStartBeat) / segmentSpan;            const rawValue = beatA <= beatB                ? valueA + (valueB - valueA) * ratio                : valueB + (valueA - valueB) * ratio;            const quantized = quantizeValue(rawValue, step);            updates.set(noteId, Math.max(min, Math.min(max, quantized)));        });        if (updates.size === 0)            return;        setProject((current) => {            let changed = false;            const nextNotesByTrack = Object.fromEntries(Object.entries(current.notesByTrack).map(([trackId, notes]) => [                trackId,                notes.map((note) => {                    const nextValue = updates.get(note.id);                    if (nextValue === undefined)                        return note;                    const currentValue = getNoteControlValue(note, key);                    if (nearlyEqual(currentValue, nextValue))                        return note;                    changed = true;                    return { ...note, [key]: nextValue };                }),            ]));            return changed ? { ...current, notesByTrack: nextNotesByTrack } : current;        });    }    function updateDetailGraphFromPointer(clientX, clientY) {        const drag = detailGraphDragRef.current;        const svg = detailGraphSvgRef.current;        if (!drag?.active || !svg)            return;        const rect = svg.getBoundingClientRect();        if (rect.width <= 0 || rect.height <= 0)            return;        const normalizedX = Math.max(0, Math.min(1, (clientX - rect.left) / rect.width));        const normalizedY = Math.max(0, Math.min(1, (clientY - rect.top) / rect.height));        const beatSpan = Math.max(0.25, drag.maxBeat - drag.minBeat);        const targetBeat = drag.minBeat + normalizedX * beatSpan;        const nearestNoteId = drag.noteIds.reduce((nearest, noteId) => {            const note = allTrackNotesById.get(noteId);            if (!note)                return nearest;            if (!nearest)                return noteId;            const nearestNote = allTrackNotesById.get(nearest);            if (!nearestNote)                return noteId;            return Math.abs(note.startBeat - targetBeat) < Math.abs(nearestNote.startBeat - targetBeat)                ? noteId                : nearest;        }, '');        if (!nearestNoteId)            return;        const rawValue = drag.max - normalizedY * (drag.max - drag.min);        const quantizedValue = clampNoteControlValue(drag.key, quantizeValue(rawValue, drag.step));        if (!drag.hasLast) {            updateSingleNoteControlValue(nearestNoteId, drag.key, quantizedValue);            drag.lastBeat = targetBeat;            drag.lastValue = quantizedValue;            drag.hasLast = true;            return;        }        updateDetailGraphSegmentValues(drag.key, drag.noteIds, drag.lastBeat, drag.lastValue, targetBeat, quantizedValue, drag.min, drag.max, drag.step);        drag.lastBeat = targetBeat;        drag.lastValue = quantizedValue;    }    function finishDetailGraphDrag(pointerId) {        const drag = detailGraphDragRef.current;        if (!drag?.active)            return;        if (pointerId !== undefined && drag.pointerId !== pointerId)            return;        const svg = detailGraphSvgRef.current;        if (svg?.hasPointerCapture(drag.pointerId)) {            svg.releasePointerCapture(drag.pointerId);        }        detailGraphDragRef.current = null;    }    function beginDetailGraphDrag(event, control, notes) {        const svg = detailGraphSvgRef.current;        if (!svg || notes.length === 0)            return;        event.preventDefault();        event.stopPropagation();        svg.setPointerCapture(event.pointerId);        const minBeat = Math.min(...notes.map((note) => note.startBeat));        const maxBeat = Math.max(...notes.map((note) => note.startBeat + note.durationBeats));        const noteIds = [...notes]            .sort((left, right) => left.startBeat - right.startBeat || left.pitch - right.pitch)            .map((note) => note.id);        detailGraphDragRef.current = {            active: true,            hasLast: false,            key: control.key,            lastBeat: minBeat,            lastValue: clampNoteControlValue(control.key, quantizeValue(getSelectedNoteValue(control.key), control.step)),            max: control.max,            maxBeat,            min: control.min,            minBeat,            noteIds,            pointerId: event.pointerId,            step: control.step,        };        updateDetailGraphFromPointer(event.clientX, event.clientY);    }    function updateAudioClipVolume(clipId, volume) {        const nextVolume = Math.round(Math.max(0, Math.min(1.5, volume)) * 100) / 100;        setProject((current) => {            let changed = false;            const audioClips = (current.audioClips ?? []).map((clip) => {                if (clip.id !== clipId || nearlyEqual(clip.volume, nextVolume))                    return clip;                changed = true;                return { ...clip, volume: nextVolume };            });            return changed ? { ...current, audioClips } : current;        });    }    function adjustAudioClipVolumeFromPointer(clip, event) {        const target = event.currentTarget;        const rect = target.getBoundingClientRect();        const setFromClientY = (clientY) => {            const ratio = 1 - Math.min(1, Math.max(0, (clientY - rect.top) / rect.height));            updateAudioClipVolume(clip.id, 0.15 + ratio * 1.35);        };        event.preventDefault();        beginHistoryBatch();        setFromClientY(event.clientY);        const handlePointerMove = (moveEvent) => {            setFromClientY(moveEvent.clientY);        };        const stop = () => {            endHistoryBatch();            window.removeEventListener('pointermove', handlePointerMove);            window.removeEventListener('pointerup', stop);            window.removeEventListener('pointercancel', stop);        };        window.addEventListener('pointermove', handlePointerMove);        window.addEventListener('pointerup', stop);        window.addEventListener('pointercancel', stop);    }    function updateNoteEvent(noteId, updates) {        setProject((current) => {            let changed = false;            const nextNotesByTrack = Object.fromEntries(Object.entries(current.notesByTrack).map(([trackId, notes]) => [                trackId,                notes.map((note) => {                    if (note.id !== noteId)                        return note;                    const nextNote = {                        ...note,                        ...updates,                        durationBeats: Math.max(MIN_DURATION_BEATS, updates.durationBeats ?? note.durationBeats),                        pitch: Math.max(0, Math.min(127, Math.round(updates.pitch ?? note.pitch))),                        startBeat: Math.max(0, updates.startBeat ?? note.startBeat),                    };                    if (note.pitch !== nextNote.pitch ||                        !nearlyEqual(note.startBeat, nextNote.startBeat) ||                        !nearlyEqual(note.durationBeats, nextNote.durationBeats) ||                        !nearlyEqual(note.velocity, nextNote.velocity)) {                        changed = true;                        return nextNote;                    }                    return note;                }),            ]));            return changed                ? {                    ...current,                    selectedNoteId: noteId,                    notesByTrack: nextNotesByTrack,                }                : current;        });    }    function resizeNote(targetNote, durationBeats) {        const trackId = getNoteTrackId(targetNote);        if (!trackId)            return;        setProject((current) => {            const currentNotes = current.notesByTrack[trackId] ?? [];            let changed = current.selectedNoteId !== targetNote.id;            const nextNotes = currentNotes.map((note) => {                if (note.id !== targetNote.id)                    return note;                const nextDurationBeats = Math.max(MIN_DURATION_BEATS, Math.min(totalBeats - note.startBeat, durationBeats));                if (nearlyEqual(note.durationBeats, nextDurationBeats))                    return note;                changed = true;                lastNoteDurationRef.current = nextDurationBeats;                return {                    ...note,                    durationBeats: nextDurationBeats,                };            });            if (!changed)                return current;            return {                ...current,                selectedNoteId: targetNote.id,                notesByTrack: {                    ...current.notesByTrack,                    [trackId]: nextNotes,                },            };        });    }    function startResizingNote(note, event) {        const row = event.currentTarget.closest('.step-row');        if (!(row instanceof HTMLElement))            return;        event.preventDefault();        event.stopPropagation();        setResizingNoteId(note.id);        beginHistoryBatch();        setProject((current) => current.selectedNoteId === note.id ? current : { ...current, selectedNoteId: note.id });        const rowRect = row.getBoundingClientRect();        let pendingClientX = event.clientX;        let resizeFrameId = 0;        function applyPendingResize() {            resizeFrameId = 0;            const x = Math.min(Math.max(pendingClientX - rowRect.left, 0), rowRect.width);            const step = Math.ceil((x / rowRect.width) * totalSteps);            const endBeat = step / stepsPerBeat;            resizeNote(note, endBeat - note.startBeat);        }        function handlePointerMove(moveEvent) {            pendingClientX = moveEvent.clientX;            if (resizeFrameId === 0) {                resizeFrameId = window.requestAnimationFrame(applyPendingResize);            }        }        function stopResizing() {            if (resizeFrameId !== 0) {                window.cancelAnimationFrame(resizeFrameId);                applyPendingResize();            }            endHistoryBatch();            setResizingNoteId(null);            window.removeEventListener('pointermove', handlePointerMove);            window.removeEventListener('pointerup', stopResizing);        }        window.addEventListener('pointermove', handlePointerMove);        window.addEventListener('pointerup', stopResizing);    }    function copySelectedNotes() {        if (!selectedTrack || selectedPatternNotes.length === 0)            return;        const sortedNotes = [...selectedPatternNotes].sort((left, right) => (left.startBeat - right.startBeat || left.pitch - right.pitch));        const firstBeat = Math.min(...sortedNotes.map((note) => note.startBeat));        const lastBeat = Math.max(...sortedNotes.map((note) => note.startBeat + note.durationBeats));        patternClipboardRef.current = {            nextPasteBeat: lastBeat,            notes: sortedNotes.map((note) => ({                ...note,                startBeat: snapBeatToGrid(note.startBeat - firstBeat),                durationBeats: snapBeatToGrid(note.durationBeats),            })),            sourceTrackId: selectedTrack.id,        };        setEditMenuOpen(false);    }    function cutSelectedNotes() {        if (!selectedTrack || selectedPatternNotes.length === 0)            return;        copySelectedNotes();        deleteSelectedNotes();        setEditMenuOpen(false);    }    function pasteSelectedNotes() {        if (!selectedTrack || !patternClipboardRef.current)            return;        const clipboard = patternClipboardRef.current;        const currentBeat = getCurrentPlaybackBeat();        const requestedPasteBeat = currentBeat > FLOAT_EPSILON ? currentBeat : clipboard.nextPasteBeat;        const pasteBeat = clampBeatToSong(snapBeatToGrid(requestedPasteBeat));        const pastedNotes = clipboard.notes.map((note) => ({            ...note,            id: createId('note'),            startBeat: clampBeatToSong(snapBeatToGrid(pasteBeat + note.startBeat), note.durationBeats),        }));        const pastedIds = pastedNotes.map((note) => note.id);        const pastedEndBeat = Math.max(...pastedNotes.map((note) => note.startBeat + note.durationBeats));        patternClipboardRef.current = {            ...clipboard,            nextPasteBeat: pastedEndBeat,        };        setSelectionBox(null);        setSelectedNoteIds(pastedIds);        setProject((current) => ({            ...current,            selectedNoteId: pastedIds[0] ?? null,            notesByTrack: {                ...current.notesByTrack,                [selectedTrack.id]: [...(current.notesByTrack[selectedTrack.id] ?? []), ...pastedNotes],            },        }));        setEditMenuOpen(false);    }    function duplicateSelectedNotes() {        copySelectedNotes();        pasteSelectedNotes();    }    function focusTrackAtBeat(trackId, beat) {        selectTrack(trackId);        setActiveEditorTab('piano-roll');        seekPlayback(beat);    }    function setPlaybackPosition(beat) {        const nextBeat = Math.max(0, Math.min(totalBeats, beat));        playbackBeatRef.current = nextBeat;        setPlaybackBeat(nextBeat);        pianoRollRef.current?.style.setProperty('--playhead-left', `${Math.min(100, (nextBeat / totalBeats) * 100)}%`);    }    function keepPlayheadInView(beat) {        const roll = pianoRollRef.current;        if (!roll || totalBeats <= 0)            return;        const gridWidth = Math.max(totalSteps * stepWidth, roll.scrollWidth - KEY_COLUMN_WIDTH);        const playheadX = KEY_COLUMN_WIDTH + (beat / totalBeats) * gridWidth;        const viewportLeft = roll.scrollLeft;        const viewportRight = viewportLeft + roll.clientWidth;        const rightEdge = viewportRight - PLAYHEAD_SCROLL_PADDING;        const leftEdge = viewportLeft + KEY_COLUMN_WIDTH + PLAYHEAD_SCROLL_PADDING;        if (playheadX > rightEdge) {            roll.scrollLeft = Math.min(roll.scrollWidth - roll.clientWidth, playheadX - roll.clientWidth + PLAYHEAD_SCROLL_PADDING);            return;        }        if (playheadX < leftEdge) {            roll.scrollLeft = Math.max(0, playheadX - KEY_COLUMN_WIDTH - PLAYHEAD_SCROLL_PADDING);        }    }    function seekPlayback(beat) {        setPlaybackPosition(beat);        keepPlayheadInView(beat);        if (isPlaying) {            void startPlaybackAt(beat);        }    }    function seekPlaybackFromTimeline(event) {        if (event.button !== 0)            return;        event.preventDefault();        const rect = event.currentTarget.getBoundingClientRect();        const x = Math.min(Math.max(event.clientX - rect.left, 0), rect.width);        seekPlayback((x / rect.width) * totalBeats);    }    function getBeatFromHorizontalPointer(event) {        const rect = event.currentTarget.getBoundingClientRect();        const x = Math.min(Math.max(event.clientX - rect.left, 0), rect.width);        return (x / rect.width) * totalBeats;    }    function addAutoMixSectionFromPointer(event) {        if (event.button !== 0)            return;        if (event.target.closest('.automix-cut-marker'))            return;        event.preventDefault();        addAutoMixSectionAtBeat(getBeatFromHorizontalPointer(event));    }    function changeTempoFromGraph(event) {        if (event.button !== 0)            return;        event.preventDefault();        const rect = event.currentTarget.getBoundingClientRect();        const y = Math.min(Math.max(event.clientY - rect.top, 0), rect.height);        const ratio = 1 - y / rect.height;        const nextTempo = Math.round(TEMPO_GRAPH_MIN + ratio * (TEMPO_GRAPH_MAX - TEMPO_GRAPH_MIN));        updateTempo(nextTempo);    }    function zoomRoll(direction) {        setRollZoom((current) => getNextZoom(current, direction));    }    const renderTempoSectionOverlay = () => (_jsx(TempoSectionOverlay, { focusTempoSection: focusTempoSection, selectedTempoSectionId: selectedTempoSection?.id ?? null, tempoSections: tempoSections, totalBeats: totalBeats }));    const renderAutoMixCutOverlay = () => (_jsx(AutoMixCutOverlay, { autoMixMarkerSections: autoMixMarkerSections, focusAutoMixSection: focusAutoMixSection, selectedAutoMixSectionId: selectedAutoMixSection?.id ?? null, totalBeats: totalBeats }));    const { activeNoteControl, detailGraphBounds, detailGraphNotes } = useEditorDerived(activeDetailTerm, sortedEditableSelectedNotes);    const noteEditing = useNoteEditing({        adjustAudioClipVolumeFromPointer,        beginMoveNote,        beginRightEraseNote,        beginRowAction,        deleteNote,        deleteSelectedNote,        deleteSelectedNotes,        resizeNote,        startResizingNote,        transposeSelectedNotes,        updateNoteEvent,        updateSingleNoteControlValue,    });    const pointerInteractions = usePointerInteractions({        beginCellAction,        beginRightClickRollAction,        cacheRollPointerGeometry,        getCellFromPointer,        openTrackContextMenu,        openTrackPanelContextMenu,    });    const keyboardShortcuts = useKeyboardShortcuts({        copySelectedNotes,        cutSelectedNotes,        deleteSelectedNote,        duplicateSelectedNotes,        pasteSelectedNotes,        redoProject,        undoProject,    });    const playbackScroll = usePlaybackScroll({        focusTrackAtBeat,        keepPlayheadInView,        seekPlayback,        seekPlaybackFromTimeline,        setPlaybackPosition,    });    const patternEditing = usePatternEditing({        beginPatternRepeat,        beginPatternSelection,        commitPatternRepeatGap,        copySelectedNotes,        cutSelectedNotes,        finishPatternRepeat,        pasteSelectedNotes,        ungroupSelectedPattern,        updatePatternRepeatPreview,    });    const selectionTools = useSelectionTools({        beginSelectionBoxMove,        findEditableNoteAtCell,        getNoteDisplayLabel,        getNoteTrackId,        getRowLabel,    });    void noteEditing;    void pointerInteractions;    void keyboardShortcuts;    void playbackScroll;    void patternEditing;    void selectionTools;    return (_jsx(EditorLayout, { activeEditorTab: activeEditorTab, arrangeViewProps: {            addAutoMixSectionFromPointer,            addTrackPlacement,            arrangedProject,            beginTrackPlacementDrag,            deleteTrackPlacement,            focusTrackAtBeat,            project,            renderAutoMixCutOverlay,            rollSurfaceStyle,            selectTrack,            selectedTrackId: selectedTrack?.id,            selectedTrackPlacementId,            totalBeats,            visibleBars,        }, autoMixPanelProps: {            addAutoMixSection,            applyAutoMixGenrePreset,            autoMixGenrePreset,            autoMixReport,            deleteAutoMixSection,            focusAutoMixSection,            getAutoMixFocusTrackId,            isAutoMixing,            project,            renderAutoMixCutOverlay,            runAutoMixTracks,            selectedAutoMixSectionId: selectedAutoMixSection?.id ?? null,            setAutoMixFocusTrack,            updateAutoMixSection,        }, canRedo: canRedo, canUndo: canUndo, closePianoRollContextMenu: closePianoRollContextMenu, closeTrackContextMenu: closeTrackContextMenu, contextMenuActions: {            addTrack,            closeTrackContextMenu,            copySelectedNotes,            cutSelectedNotes,            cycleTrackColor,            deleteSelectedNote,            deleteTrack,            openTrackProperties: (trackId) => {                const track = project.tracks.find((item) => item.id === trackId);                if (track)                    openInstrumentDialog(track);            },            pasteSelectedNotes,            redoProject,            undoProject,        }, detailPanelProps: {            activeDetailTerm,            activeNoteControl,            beginDetailGraphDrag,            changeTempoInput,            commitTempoInput,            detailGraphBounds,            detailGraphDragRef,            detailGraphNotes,            detailGraphSvgRef,            detailPanelOpen,            editableSelectedNotes,            finishDetailGraphDrag,            getNoteControlValue,            getSelectedNoteValue,            isPlaying,            projectTempo: project.tempo,            resetPlayback,            selectedNote,            setActiveDetailTerm,            setDetailPanelOpen,            snapBeatToGrid,            sortedEditableSelectedNotes,            tempoInput,            togglePlayback,            updateDetailGraphFromPointer,            updateNoteEvent,            updateTempo,        }, instrumentDialogProps: {            categoryInstruments,            closeInstrumentDialog,            confirmInstrumentDialog,            instrumentCategories,            instrumentCategory,            instrumentDialogTrack,            previewInstrumentChoice,            selectedInstrumentId,            setInstrumentCategory,        }, isAutoMixing: isAutoMixing, isDraggingFile: isDraggingFile, onDragLeave: () => setIsDraggingFile(false), onDragOver: handleDragOver, onDrop: handleDrop, patternClipboardExists: Boolean(patternClipboardRef.current), selectedPatternNoteCount: selectedPatternNotes.length, pianoRollContextMenu: pianoRollContextMenu, pianoRollProps: {            adjustAudioClipVolumeFromPointer,            allTrackMelodyMode,            beatWidth,            beginKeyPreview,            beginMoveNote,            beginPatternRepeat,            beginRightEraseNote,            beginRowAction,            beginRowContextErase,            beginSelectionBoxMove,            changeToolMode,            commitPatternRepeatGap,            continueKeyPreview,            draggingNoteId,            getNoteDisplayLabel,            getRowLabel,            keyboardInputEnabled,            lassoPoints,            noteDivision,            openInstrumentDialog,            otherNotesByPitch,            patternRepeatGapInput,            pianoRollRef,            playbackPressedPitchCounts: playbackPressedPitchCountsRef.current,            pressedPitch,            projectSelectedNoteId: project.selectedNoteId,            renderAutoMixCutOverlay,            resizingNoteId,            rollPitches,            rollShellStyle,            rollTimelineStyle,            rollZoom,            seekPlaybackFromTimeline,            selectedAudioClips,            selectedNoteIdSet,            selectedNoteIds,            selectedNotesByPitch,            selectedPatternRepeatGroup,            selectedTrack,            selectedTrackIsAudio,            selectionBox,            setKeyboardInputEnabled,            setNoteDivision,            setPatternRepeatGapInput,            startResizingNote,            toolMode,            totalBeats,            ungroupSelectedPattern,            visibleBars,            zoomRoll,        }, projectTheme: project.theme, tempoPanelProps: {            addTempoSection,            changeTempoFromGraph,            changeTempoInput,            commitTempoInput,            deleteTempoSection,            focusTempoSection,            projectTempo: project.tempo,            renderAutoMixCutOverlay,            renderTempoSectionOverlay,            rollSurfaceStyle,            selectedTempoSection,            setSelectedTempoSectionId,            tempoInput,            tempoSections,            updateTempo,            updateTempoSection,        }, toolMode: toolMode, topMenuProps: {            activeEditorTab,            audioFileInputRef,            canRedo,            canUndo,            copySelectedNotes,            createNewProject,            cutSelectedNotes,            duplicateSelectedNotes,            editMenuOpen,            fileInputRef,            fileMenuOpen,            hasPatternClipboard: Boolean(patternClipboardRef.current),            importAudioFiles,            isExportingMp3,            loadProjectFile,            openProjectFile,            pasteSelectedNotes,            projectTitle: project.title,            redoProject,            saveMidiFile,            saveMp3File,            saveProjectFile,            selectedPatternNoteCount: selectedPatternNotes.length,            setActiveEditorTab,            setAutoMixPanelOpen,            setEditMenuOpen,            setFileMenuOpen,            undoProject,            updateProjectTitle,        }, trackContextMenu: trackContextMenu, trackPanelProps: {            addAutoMixSection,            addTrack,            allTrackMelodyMode,            applyAutoMixGenrePreset,            autoMixGenrePreset,            autoMixPanelOpen,            autoMixReport,            deleteAutoMixSection,            focusAutoMixSection,            getAutoMixFocusTrackId,            isAutoMixing,            isRecordingVoice,            openAudioUpload,            openInstrumentDialog,            openTrackContextMenu,            openTrackPanelContextMenu,            project,            runAutoMixTracks,            selectTrack,            selectedAutoMixSectionId: selectedAutoMixSection?.id ?? null,            selectedTrackName: selectedTrack?.name ?? '?몃옓 1',            setAllTrackMelodyMode,            setAutoMixFocusTrack,            setAutoMixPanelOpen,            toggleVoiceRecording,            totalBeats,            updateAutoMixSection,            updateTrack,        }, tracksCount: project.tracks.length }));}